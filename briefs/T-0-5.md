# BRIEF · T-0-5 · Read-only пользователь Firebird + GRANT SELECT

**Кем исполняется (ADR-030):** исполнитель · модель sonnet (поле `model` в `.claude/agents/executor.md`)

**Класс задачи (ADR-018):** B
Задача обращается к БД PawnShop и работает за авторизацией на сервере клиента. Класс совпадает с
таблицей мандата `07_STATE` — расхождения нет. Каждый шаг с изменением исполняется ТОЛЬКО после
отдельного подтверждения Ilyas, и перед запросом подтверждения идёт отдельное сообщение: что
исполняется, на каком объекте, чем откатывается.

**Параллель:** нет
(буфер и проход сборки не заводятся — `ADR-031`; задачи идут по одной)

**Файлы на запись** (полный список; на нём МЕХАНИЧЕСКИ проверяется пересечение):
- `scripts/grant_ro.sql` — пере-запускаемый скрипт выдачи прав на чтение, без паролей и путей
- `reference/T-0-5_readonly_measurement_<дата>.md` — артефакт замера: логи всех проверок приёмки
  (`<дата>` — локальная дата дня в формате `YYYY-MM-DD`)
- `11_INFRA_FACTS.md` — INFRA_PATCH: путь к файлу базы и факт существования read-only пользователя

## Роль
Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Модель исполнения: команды на сервере запускает Ilyas по RDP и возвращает логи; ты пишешь команду
и ожидаемый вывод, читаешь лог, решаешь. `Done` — только по предъявленному логу, не по коду
возврата и не по «команда отработала».

## Цель
На сервере Firebird существует отдельный пользователь с правом ТОЛЬКО на чтение девяти нужных
таблиц, и предъявлен лог, что под ним: (а) чтение этих таблиц работает; (б) чтение системных
`RDB$`-таблиц работает — без него не работает guard схемы (`ADR-004`); (в) чтение НЕ выданной
таблицы отбивается ошибкой прав; (г) попытка записи отбивается ошибкой прав. После задачи
регулярные обращения к базе идут только под этим пользователем.

## Context-to-load (обязательно прочитать перед работой; файлы читаются С ДИСКА по именам)
- обязательный минимум — по единственному списку из `CLAUDE.md`
- `00_CHARTER.md` `§4` — красные линии: из PawnShop только чтение
- `11_INFRA_FACTS.md` — контур клиента: сервер, порт, путь установки Firebird
- `06_DECISIONS_LOG.md` — точечно: `ADR-001` санитария · `ADR-004` guard схемы (нужен доступ к
  `RDB$`) · `ADR-018` мандат · `ADR-029` ротация не выполняется, эта задача — единственная
  оставшаяся мера
- `briefs/T-0-5.md` — этот файл, раздел «Список таблиц»
Чего-то из списка нет → `CONTEXT GAP` и остановка.

## Входы

- **Доступ по RDP** к серверу ERP через туннель — у Ilyas.
- **Административные креды БД и путь к файлу базы** — у Ilyas. В бриф, логи и артефакты не
  пишутся. Нет доступа → `CONTEXT GAP`.
- **Пароль нового пользователя** — генерирует и хранит Ilyas в своём менеджере паролей. В
  репозиторий, бриф и логи — никогда. Secret Manager ещё не создан (`T-0-7`); после `T-0-7`
  значение переносится туда.
- **Ничего устанавливать не нужно** — ни на сервере, ни на машине Ilyas. Обе требуемые программы
  (`gsec.exe` для пользователей, `isql.exe` для запросов) входят в установку Firebird на сервере и
  уже использовались задачей `T-0-2`.

## Список таблиц — ВНИМАНИЕ, источник подставлен, и это названо

Прежняя редакция брифа брала список из `connector/mapping.json`, секция `schema_fingerprint`.
**Замер 2026-08-08: этого файла в репозитории НЕТ**; `02_DATA_CONTRACTS §3` пуст (все строки `⏳`).
Причина известна и записана: вендорские артефакты уезжают в приватный сателлит задачей `T-0-10`,
которая не исполнена. Поэтому источником списка объявляется ЭТОТ бриф, а не отсутствующий файл:

1. `CONTRACTS`
2. `CONTRACTS_TERMS`
3. `SUBJECTS`
4. `CUSTOM_FIELDS_VALUES`
5. `DIR_CUSTOM_FIELDS`
6. `TABLES_TABLE`
7. `CONTRACT_STATES`
8. `DEPOSIT_TYPES`
9. `OPERATIONS` — нужна для `T-0-4`, право выдаётся заранее

**Таблицы вне этого списка не грантуются.** Риск принят осознанно и мал: скрипт выдачи прав
пере-запускаемый, и если после `T-0-10` эталон отпечатка добавит таблицы, скрипт прогоняется
заново. Расширять список догадкой запрещено — новая таблица приходит правкой скрипта с названной
причиной.

## Ограничения

- **К данным PawnShop — только чтение** (`00 §4`). Создание пользователя и выдача прав на чтение —
  единственное разрешённое исключение, прямо предусмотренное конвенциями и планом. Никаких иных
  изменений схемы или данных: ни удаления, ни изменения объектов вендора, ни правки существующих
  пользователей вендора.
- `SELECT *` запрещён — везде явные списки колонок.
- **Санитария `ADR-001`:** в логах и session-блоке нет паролей и нет персональных данных клиентов.
  Для проверок достаточно `COUNT(*)`, значения строк не выводятся.
- **Не-идемпотентные шаги:** обрыв связи во время создания пользователя → слепой повтор ЗАПРЕЩЁН.
  Сначала read-only диагностика (просмотр списка пользователей), потом доисполнение только
  недостающего хвоста.
- **Недостоверный вывод:** побитый при копировании лог из RDP фактом не является. Не примирять
  противоречия догадкой — запросить чистый повторный лог.

## Особенности Firebird 2.5, учесть обязательно

1. **Пользователи общесерверные, а не внутри базы.** Живут в служебной базе безопасности в
   каталоге установки Firebird. То есть создание пользователя — запись в служебную базу сервера
   вендора, а выдача прав — запись в метаданные рабочей базы. Оба факта зафиксировать в логе явно.
2. **Старый режим проверки пароля значимо учитывает только первые 8 символов.** Длинный пароль не
   даёт длинной защиты. Это **проверяется замером**, а не принимается на веру (шаг 5-д).
   Практическое следствие: основной контроль доступа — правило файрвола на порту базы.
3. **Обновление ERP может переустановить Firebird и потерять служебную базу безопасности** —
   пользователь исчезнет. Поэтому скрипт пере-запускаемый, а будущий коннектор обязан падать
   громко на ошибке аутентификации, а не молчать. Вписать в «что вернуть» как вход для `T-0-8`.
4. **Права на системные `RDB$`-таблицы** в этой версии обычно доступны всем, но это
   **подтверждается замером** (шаг 5-б). Без чтения `RDB$` guard схемы не работает, и тогда нужен
   отдельный грант либо пересмотр `ADR-004`.

## Шаги

Каждый шаг — одна команда для запуска Ilyas по RDP, с напечатанным ожидаемым выводом.
**Шаг 1 read-only. Шаги 2 и 3 меняют состояние сервера — перед каждым отдельное сообщение
подтверждения по `ADR-018`.**

1. **Диагностика до любых изменений.** Список существующих пользователей сервера. Цель: убедиться,
   что имени `LOMBARD_RO` ещё нет, и увидеть пользователей вендора, чтобы их не задеть. Ожидаемый
   вывод — перечень имён, среди которых административное. Имени `LOMBARD_RO` быть не должно; если
   оно есть — стоп и разбор, задача уже частично исполнялась.

2. **Создать пользователя `LOMBARD_RO`.** Пароль подставляет Ilyas. Ожидаемый вывод — пустой
   (программа молчит при успехе); подтверждение — повторный просмотр списка, где появилось имя.
   **Откат:** удаление пользователя той же программой.

3. **Выдать права на чтение** по девяти таблицам скриптом `scripts/grant_ro.sql`, по одной строке
   на таблицу. Выдача прав идемпотентна, повторный прогон безопасен. Ожидаемый вывод — без ошибок.
   **Откат:** отзыв прав тем же списком.

4. **Положить скрипт в репозиторий** как `scripts/grant_ro.sql`: после обновления ERP его гоняют
   заново, и список прав виден в ревью. Пароли и путь к базе в файл не попадают — параметризуются.

5. **Проверки под НОВЫМ пользователем — это и есть приёмка.** Все пять предъявляются логом.
   - а. `COUNT(*)` по каждой из девяти таблиц — возвращает числа.
   - б. чтение системных `RDB$`-метаданных — возвращает строки. Не вернуло → стоп, `CONTEXT GAP`
     архитектору: нужен отдельный грант или пересмотр `ADR-004`.
   - в. `COUNT(*)` по одной НЕ выданной таблице — ожидается ошибка прав. Успех вместо ошибки =
     лишние права: стоп, вернуть архитектору.
   - г. попытка изменения с предикатом, не задевающим ни одной строки — ожидается ошибка прав.
     Предикат выбран так, что даже при наличии прав ничего не меняется, поэтому проба безопасна.
     Успех вместо ошибки = есть права на запись: **немедленно стоп**, пользователя не использовать.
   - д. проверка особенности с восемью символами пароля: попытка входа со значением, совпадающим по
     первым восьми символам и различающимся дальше. Вход прошёл → особенность подтверждена, факт
     записывается в состояние; вход отбит → особенность не подтверждена, записывается тоже.

## Приёмка (сверено с `04_ROADMAP`)

Бриф перевыпущен без ссылок и незаполненных плейсхолдеров (`ADR-021`) — выполнено этим файлом.
Скрипт лежит в `/scripts`. Подключение под новым пользователем проверено — предъявлен лог по всем
пяти пунктам шага 5, включая ОБА отрицательных случая (в и г): отсутствие лога отказа означает, что
проверка не измерена, а не что она прошла.

## Что вернуть человеку

Одну строку итога, ссылку на артефакт замера и явный ответ на три вопроса: работает ли чтение
системных метаданных; подтвердилась ли особенность с восемью символами; какие таблицы остались вне
списка и почему.

## Конец сессии

Один session-блок по `05 §III`, с ролью и моделью сессии. `INFRA_PATCH` обязателен: путь к файлу
базы и факт существования пользователя (без пароля). Пустой пишется явным «нет изменений».
