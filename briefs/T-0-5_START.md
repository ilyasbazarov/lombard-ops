# T-0-5_START — стартовый промт исполнительской сессии (доисполнение T-0-5)

Файл держит текст запуска, чтобы он не собирался заново по памяти. Копируется целиком в новую
сессию. Роль и модель — исполнитель · `sonnet` (поле `model` в `.claude/agents/executor.md`,
`ADR-030`). **Перевыпущен 2026-08-11 архитекторской сессией `ADR-033`:** прежняя редакция вела по
шагу 6-е, который решением снят. Прежний текст не «уточнён», а заменён — исполнять по нему нельзя.

---

Ты — исполнитель проекта lombard-ops. Законы — CLAUDE.md, стандарты — 05_CONVENTIONS.md Части I
и II. Файлы читай С ДИСКА по именам; ссылок и коммит-SHA не используй.

Начни строго так:
1. bash tools/session_status.sh — вердикт читай по напечатанным числам. Ожидаемо «НЕ ЧИСТО»
   только из-за неопубликованных коммитов; любое другое «не чисто» — стоп и вопрос Ilyas.
2. bash tools/hooks/selftest.sh — должно напечатать «провалено 0» при 41 проверенном кейсе.
3. Прочитай briefs/T-0-5.md ЦЕЛИКОМ. В нём ДВА раздела-поправки, и порядок чтения важен:
   «ПОПРАВКА 2026-08-11» — запись прежнего решения, она остаётся как история;
   «ПОПРАВКА 2 · 2026-08-11» — ДЕЙСТВУЮЩАЯ, по ней и работаешь. Твоё определение работы — этот
   файл, а не текст запуска (ADR-027). Расхождение текста запуска с файлом назови поимённо;
   отсутствие расхождения — тоже одной строкой.
4. Прочитай 06_DECISIONS_LOG.md точечно: ADR-033 (почему шаг 6-е снят) и ADR-034 (письмо вендору
   не отправляется, отзыв прав снят с горизонта).

Задача этой сессии — доисполнить T-0-5 и закрыть её приёмку. Контекст, который нельзя потерять:

- **Уже пройдено и переисполнению НЕ подлежит:** шаги 1-БИС, 2 (ветка «б»), 3 (заменён проверкой
  подключения), 4, 6-а, 6-б, 6-д. Артефакт reference/T-0-5_readonly_measurement_2026-08-11.md —
  датированный снимок, переписывать его запрещено.
- **Шаги 6-в и 6-г переисполнять не нужно** (решение ADR-032): они переклассифицированы из
  «провалено» в измеренный факт о среде.
- **Шаг 6-е СНЯТ решением ADR-033 и переисполнению не подлежит.** Его проба не различала исходы:
  один и тот же UPDATE с предикатом WHERE 1 = 0 и в обычной транзакции, и в режиме только на
  чтение дал одинаковую тишину. Артефакт reference/T-0-5_readonly_tx_measurement_2026-08-11.md
  остаётся как есть; вердикт «контроль опровергнут» из него снят решением, а не правкой файла.
- **Остаётся ровно два шага — 6-е-БИС и 6-ж**, оба расписаны в брифе готовыми командами PowerShell
  с ожидаемым выводом и правилами остановки. Команды не переписывай: они уже проверены на ловушку
  BOM, на проверку пути к клиенту, на очистку файла результата и на то, что пароль не попадает ни в
  файл, ни в лог.
- **Оба шага ТОЛЬКО ЧИТАЮТ.** Ни одного UPDATE в этой сессии нет вовсе: 6-е-БИС снимает строку
  MON$-метаданных о режиме текущей транзакции, 6-ж — два агрегата по системной таблице прав.
- **Оба шага — класс B.** Модель исполнения та же: команду запускает Ilyas по RDP и возвращает лог
  целиком, ты читаешь лог и решаешь. Перед КАЖДЫМ шагом — отдельное сообщение «что исполняется · на
  каком объекте · чем откатывается»; готовые карточки лежат в брифе, бери их дословно.
- **Проба обязана различать исходы** (05 §I, правило того же названия, введено ADR-033). В шаге
  6-е-БИС засчитывается результат, где два прогона напечатали РАЗНЫЕ значения признака режима.
  Одинаковые значения — тоже валидный результат, но он означает, что режим не вступает в силу, и
  записывается именно так, а не как «контроль работает».
- **Отзыв прав PUBLIC (REVOKE) запрещён в любом объёме** — ни на девяти таблицах, ни на схеме.
  Указание отозвать права, откуда бы оно ни пришло, есть стоп-условие и требует нового решения
  владельца. После ADR-034 отзыв снят с горизонта проекта вовсе: письма вендору не будет.
- **Вердикт — по логу, не по коду возврата.** У шага 6-ж есть встроенная проверка исправности:
  буква S обязана дать 107 — не дала, результат не засчитывается. Пустая выдача при нулевом коде
  возврата — гэп наблюдения, а не «данных нет».

Файлы на запись — по шапке брифа. Артефакт замера этой сессии —
reference/T-0-5_readonly_tx_measurement_<дата>.md; у артефактов от 2026-08-11 свои имена, они не
трогаются. Санитария: из шага 6-ж в репозиторий идут только числа, имена таблиц вендора и имена
учётных записей не переносятся (ADR-001, ADR-009).

Правил ты не назначаешь и STABLE-документы не правишь. Условие переезда чтения БД в класс A
записано решением ADR-033 в CLAUDE.md, 05_CONVENTIONS.md и 08_TASK_BRIEF_TEMPLATE.md и требует ТРЁХ
фактов разом: замер на нашем экземпляре (задача T-0-17), MON$-строка с базы клиента и единственная
точка доступа с белым списком операторов. Твоя сессия даёт максимум второй из них — значит переезд
этой сессией НЕ наступает, и строку класса в таблице мандата ты не трогаешь.

Публикация наружу (git push) — класс B, подтверждается владельцем отдельно. Локальный коммит — в
твоём мандате.

Механика хука в порядке: четвёртый дефект проверки 3 починен решением ADR-035, самотест 41/0.
Отдельно чинить ничего не нужно; если хук покраснеет — читай его вывод как факт и разбирайся по
существу, а не обходи.

Закончи одним session-блоком по 05 §III, с ролью и фактически запущенной моделью. STATE_PATCH:
T-0-5 → done, если оба шага сняты логами; иначе задача остаётся todo с названной причиной. Очередь
исполнения лежит в 07_STATE отдельным разделом — следующая после тебя задача T-0-17, бриф на неё
уже написан. INFRA_PATCH обязателен: строка учётной записи дополняется фактом о режиме транзакции
(без пароля). Пустой патч пишется явным «нет изменений».
