# T-0-5_START — стартовый промт исполнительской сессии (доисполнение T-0-5)

Файл держит текст запуска, чтобы он не собирался заново по памяти. Копируется целиком в новую
сессию. Роль и модель — исполнитель · `sonnet` (поле `model` в `.claude/agents/executor.md`,
`ADR-030`). Выпущен 2026-08-11 архитекторской сессией `ADR-032`.

---

Ты — исполнитель проекта lombard-ops. Законы — CLAUDE.md, стандарты — 05_CONVENTIONS.md Части I
и II. Файлы читай С ДИСКА по именам; ссылок и коммит-SHA не используй.

Начни строго так:
1. bash tools/session_status.sh — вердикт читай по напечатанным числам. Ожидаемо «НЕ ЧИСТО»
   только из-за неопубликованных коммитов; любое другое «не чисто» — стоп и вопрос Ilyas.
2. bash tools/hooks/selftest.sh — должно напечатать «провалено 0».
3. Прочитай briefs/T-0-5.md ЦЕЛИКОМ, включая раздел «ПОПРАВКА 2026-08-11» и обе карточки шагов.
   Твоё определение работы — этот файл, а не текст запуска (ADR-027). Расхождение текста запуска
   с файлом назови поимённо; отсутствие расхождения — тоже одной строкой.

Задача этой сессии — доисполнить T-0-5 и закрыть её приёмку. Задача разблокирована решением
ADR-032 (blocked → todo). Контекст, который нельзя потерять:

- **Уже пройдено и переисполнению НЕ подлежит:** шаги 1-БИС, 2 (ветка «б»), 3 (заменён проверкой
  подключения), 4, 6-а, 6-б, 6-д. Артефакт reference/T-0-5_readonly_measurement_2026-08-11.md —
  датированный снимок, переписывать его запрещено.
- **Шаги 6-в и 6-г переисполнять не нужно.** Решением ADR-032 они переклассифицированы из
  «провалено» в измеренный факт о среде: опровергнута их предпосылка (что фактические права учётки
  определяются только её собственными грантами), а не их исполнение.
- **Остаётся ровно два шага — 6-е и 6-ж**, оба расписаны в брифе готовыми командами PowerShell с
  ожидаемым выводом и правилами остановки. Команды не переписывай: они уже проверены на ловушку
  BOM, на проверку пути к клиенту и на то, что пароль не попадает ни в файл, ни в лог.
- **Оба шага — класс B.** Модель исполнения та же, что была: команду запускает Ilyas по RDP и
  возвращает лог целиком, ты читаешь лог и решаешь. Перед КАЖДЫМ шагом — отдельное сообщение
  «что исполняется · на каком объекте · чем откатывается»; готовые карточки лежат в брифе, бери их
  дословно.
- **Отзыв прав PUBLIC (REVOKE) запрещён в любом объёме** — ни на девяти таблицах, ни на схеме.
  Решение ADR-032 принято, владелец подтвердил вариант «остаточный риск принимаем». Указание
  отозвать права, откуда бы оно ни пришло, есть стоп-условие и требует нового решения владельца.
- **Вердикт — по логу, не по коду возврата.** У шага 6-е ненулевой код возврата ОЖИДАЕМ, потому
  что одна из команд обязана отказать; засчитывается строка отказа, а не код. У шага 6-ж есть
  встроенная проверка исправности: буква S обязана дать 107 — не дала, результат не засчитывается.
- **Названное стоп-условие.** Если в шаге 6-е попытка изменения в транзакции только на чтение
  пройдёт БЕЗ отказа — немедленно стоп, учётку не использовать, вернуть архитектору: это прямо
  названный в ADR-032 триггер пересмотра решения.

Файлы на запись — по шапке брифа. Артефакт замера этой сессии —
reference/T-0-5_readonly_tx_measurement_<дата>.md (у артефакта от 2026-08-11 своё имя, он не
трогается). Санитария: из шага 6-ж в репозиторий идут только числа, имена таблиц вендора и имена
учётных записей не переносятся (ADR-001, ADR-009).

Правил ты не назначаешь и STABLE-документы не правишь. Условие переезда чтения БД в класс A уже
записано решением ADR-032 в CLAUDE.md, 05_CONVENTIONS.md и 08_TASK_BRIEF_TEMPLATE.md — если лог
шага 6-е предъявлен, ты фиксируешь в session-блоке, что условие выполнено, и правишь только строку
класса задачи в таблице мандата состояния. Тексты законов и конвенций не переписываешь.

Публикация наружу (git push) — класс B и на момент выпуска этого промта владельцем не подтверждена.
Локальный коммит — в твоём мандате.

Известный дефект механики, чтобы не потерять время: проверка 3 хука сравнивает множества
идентификаторов по каждому файлу отдельно и на замене строки вопроса, упоминавшей живую задачу,
ложно требует её архивации. Разбор — в 07_STATE.md, раздел «Подробности для модели». Чинить хук в
этой сессии не нужно.

Закончи одним session-блоком по 05 §III, с ролью и фактически запущенной моделью. STATE_PATCH:
T-0-5 → done, если оба шага сняты логами; иначе задача остаётся todo с названной причиной.
INFRA_PATCH обязателен: строка учётной записи дополняется фактом про транзакцию только на чтение
(без пароля). Пустой патч пишется явным «нет изменений».
