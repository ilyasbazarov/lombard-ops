# 07 · ARCHIVE — Закрытые строки состояния

**Статус:** LIVE, append-only. Строка, полностью закрытая в `07_STATE.md`, переносится сюда
ДОСЛОВНО тем же коммитом (ADR-019). Пересказ вместо переноса — дефект.

## Перенесено 2026-08-12 (задача `T-0-7`, исполнена) — строка очереди исполнения

| 2 | `T-0-7` GCP-фундамент | **B** | **готов**: `briefs/T-0-7.md` (2026-08-12), восемь шагов, метка `продукт` | **На критическом пути к дате 31 августа и не зависит ни от чего** (`04`, столбец зависимостей — прочерк). Держится единственным решением владельца — регион. Пока не исполнена, живой алерт невозможен физически: ни конвейеру, ни функциям, ни планировщику негде работать. Позиция назначена сессией как рекомендация; **порядок утверждает владелец** |

Замер закрытия — `reference/T-0-7_gcp_foundation_measurement_2026-08-12.md`.

## Перенесено 2026-08-07 (задача METHOD-UPGRADE) — раздел «Сделано»

- Репо создан, стартовый пак документации закоммичен.
- GCP-проект клиента существует (факты — `11_INFRA_FACTS`).
- Ответы вендора «Алгоритм» получены: Firebird 2.5.9, порт 3057, UTF-8, начисления не в БД, схема без документации и мутирует (→ ADR-004, ADR-006).
- WG-туннель до контура клиента поднят и проверен (RDP до ERP-сервера работает).
- Служба Firebird найдена: `FirebirdServerPawnShop-3057`, слушает 0.0.0.0:3057; путь `C:\Program Files (x86)\Firebird-2.5-PawnShop\`.
- Firewall-правило для 3057 (VPN+LAN) — в работе (T-0-1, финальный ретест с Мака).
- Админ-креды Windows Server у Ilyas.
- **Структура БД PawnShop снята** (T-0-2 done): 108 таблиц, 1018 колонок; дамп передан архитектору в чат.
- **T-0-3 done:** структура и confirm-данные готовы; приёмка по 04_ROADMAP выполнена (02 §3 + драфт mapping.json). Контракт расчёта balance_amount (формула хранения, сверка портфеля 82/98, оракул на 6 договорах) выделен в отдельную задачу T-0-3b.
- **Гипотеза STORAGE_VAL подтверждена независимым источником (Иса) и закрыта решением ADR-010 v3:** хранение — полноправная компонента balance_amount, реализуется в v1.

## Перенесено 2026-08-08 — задача T-0-16 (гигиена репозитория), исполнена

Дубль записи в журнале изменений снят (9 строк, совпадение блоков проверено побайтово). Сплошной
поиск по трём категориям санитарии `ADR-001` проведён, по каждому совпадению принято решение;
раскрытые значения изъяты из рабочего дерева, синтетическая фикстура проверки оставлена намеренно.
Замер — `reference/T-0-16_sanitation_sweep_2026-08-08.md`. История репозитория значения сохраняет:
это принятый риск (`ADR-029`), а не незакрытый пункт задачи.

| T-0-16 гигиена репозитория | todo | A |

## Перенесено 2026-08-08 (решение ADR-031) — вопрос Q-12 и задача M-6b (буфер и проход сборки)

**Закрыто отказом, а не отсрочкой.** Буфер `reference/_inbox/`, роль прохода сборки и режимы
параллели не заводятся: параллельных сессий в проекте не было ни разу, а единственная часть связки
с самостоятельной ценностью — скрипт проверки пересечения — уже написана задачей `M-6a` и лежит в
`tools/`. **Условие оживления:** две задачи класса A в очереди одновременно, которые владелец хочет
вести параллельно, и подтверждённое скриптом непересечение их рабочих наборов. Закон «одно рабочее
дерево на сессию» этим НЕ отменяется — он принят отдельно и остаётся в силе.

| Q-12 | DEFER | Заводить ли буфер session-блоков и проход сборки (отложенная часть ADR-025; скрипт из строки ушёл — ADR-028) | две задачи класса A с объявленными непересекающимися наборами файлов; проверяется скриптом задачи M-6a |

| Q-12 | DEFER | Заводить ли буфер `reference/_inbox/` и роль прохода сборки (отложенная часть ADR-025). **Скрипт проверки пересечения из строки ВЫШЕЛ решением ADR-028** — он есть инструмент замера этого же гейта и приходит задачей `M-6a` без гейта | Механизмы сериализуют запись в состояние при нескольких потоках; при последовательной очереди дают ноль, а стоят отдельной сессии на пачку. Замер на 2026-08-07: деревьев 1, веток `s/*` 0, обе заявленные в состоянии пары содержат задачу класса B и не параллелятся вовсе. Закон «одно дерево на сессию» принят отдельно и в эту строку НЕ входит | две задачи класса A с объявленными непересекающимися наборами файлов; часть (iii) меряется скриптом задачи `M-6a`, а совпадение по общему сериализуемому ресурсу (состояние, реестр, журнал, оглавление, архив) пересечением НЕ считается — оно требует буфера, то есть самой этой строки | нерешённое решение |

| M-6b отложенная часть изоляции: буфер, проход сборки | todo | A (не начинать до снятия гейта ADR-025) |

## Перенесено 2026-08-08 — вопрос Q-1 и задача T-0-13 (параллельная инсталляция)

**Закрыто ответом владельца 2026-08-08:** второго филиала нет. Приёмка задачи требовала ровно
этого — зафиксировать ответ и закрыть строку вопроса; ответ получен от владельца напрямую, поэтому
задача закрывается вместе с вопросом, а не исполняется отдельной сессией.
**Следствие:** триггер пересмотра архитектуры на multi-tenant (`01 §6`) НЕ срабатывает. Найденный
на сервере каталог параллельной инсталляции считается устаревшей или тестовой копией; в конвейер он
не входит и источником данных не является. Тема закрыта и заново не поднимается.

| Q-1 | OPEN | Параллельная инсталляция `D:\PawnShop_SOM` с активностью до 01.07.2026 — второй филиал, тестовая копия или устаревшее? | При втором филиале срабатывает триггер пересмотра архитектуры `01 §6` (multi-tenant) | T-0-13 | недостающий факт |

| T-0-13 | Статус параллельной инсталляции: второй филиал, тест или устаревшее | ответ Исы/Бектура зафиксирован; строка `Q-1` закрыта или переведена в `DEFER` с гейтом | — |

| T-0-13 статус параллельной инсталляции | todo | A |

## Перенесено 2026-08-08 (решение ADR-029) — задача M-5 и блокер по учётной записи БД

Задача заморожена решением владельца: к ротации до конца проекта не возвращаемся, значение не
меняется. Риск принят сознательно. Обе строки перенесены ДОСЛОВНО, как требует `ADR-019`.

| Задача | Статус | Класс мандата |
|---|---|---|
| M-5 ротация пароля БД | todo | **B** |

- **БД считается скомпрометированной в пределах LAN/VPN-контура.** Пароль администратора дефолтный и раскрыт в публичной истории. Раскрытие шире, чем фиксировалось ранее: значение присутствует в журнале изменений **дважды** (запись продублирована целиком), в брифе `T-0-2` литералом внутри команды и в **8 коммитах** публичной истории. Удаление файлов проблему НЕ закрывает — история публичного репозитория уже роздана. Единственное закрытие — смена пароля на стороне сервера (`M-5`). Проверка на секреты в хуке (`M-4`) закрывает повторение, но не этот случай. Прод-конвейер под административным пользователем не запускать. Формулировка не ослабляется до предъявления лога отказа подключения старым значением.

Строка роудмапа задачи `M-5`, перенесённая тем же коммитом:

| M-5 | Ротация пароля администратора БД | **B** | подключение старым значением отказано — лог с текстом отказа; новым — успешно, лог; строка блокера в состоянии переписана фактом |

**Что из перенесённого остаётся в силе как факт, а не как задача:** значение действующее и
раскрытое; прод-конвейер под административной учётной записью не запускается — это переходит в
`T-0-5` (read-only пользователь), которая после `ADR-029` есть единственная оставшаяся мера.

## Перенесено 2026-08-11 (решение ADR-032) — строка вопроса Q-14 и блокер по правам PUBLIC

Вопрос закрыт решением, а не отсрочкой: права псевдо-пользователя вендора не отзываются, контроль
записи переносится в наш слой. Строки перенесены ДОСЛОВНО, как требует `ADR-019`.

Строка индекса из `07_STATE.md`:

| Q-14 | OPEN | Отзывать ли гранты `PUBLIC` (полный CRUD) на таблицы БД PawnShop, чтобы `LOMBARD_RO` был read-only фактически, а не только по своим собственным грантам | решение архитектора/владельца — масштаб всей схемы (≥9 наших таблиц + минимум 98 прочих), риск для самого приложения PawnShop |

Полная строка из `07_GAPS.md`:

| Q-14 | OPEN | Отзывать ли гранты `PUBLIC` (SELECT/INSERT/UPDATE/DELETE/REFERENCES) на таблицы БД PawnShop, чтобы `LOMBARD_RO` был read-only фактически | Замер `T-0-5` (2026-08-11) подтвердил живым `UPDATE`: `LOMBARD_RO` пишет в БД, хотя сам получил только `SELECT` — причина в `PUBLIC`, а не в скрипте выдачи прав. Красная линия `00 §4` не обеспечена на уровне СУБД, пока это не решено | решение архитектора/владельца — отзыв затрагивает минимум 9+98 таблиц и потенциально само приложение PawnShop (вдруг оно тоже подключается без явных грантов, полагаясь на PUBLIC) | недостающее решение |

Блокер, перенесённый тем же коммитом:

- **`T-0-5` заблокирована находкой замера 2026-08-11.** Собственные гранты `LOMBARD_RO`
  (`SELECT` на 9 таблиц) корректны и подтверждены, но псевдо-пользователь `PUBLIC` несёт
  `D/I/R/S/U` на эти же 9 таблиц и минимум ещё на 98 — это политика вендора на уровне всей БД,
  выданная `SYSDBA` до этой задачи. Живой замер (`UPDATE DEPOSIT_TYPES ... WHERE 1 = 0` прошёл
  БЕЗ ошибки прав) подтвердил: `LOMBARD_RO` фактически может писать, красная линия `00 §4`
  («к БД PawnShop — только чтение») не обеспечена на уровне СУБД. Отзыв прав `PUBLIC` — вне
  мандата класса A исполнителя (масштаб всей схемы, риск для приложения PawnShop), решение —
  `Q-14`. Замер целиком — `reference/T-0-5_readonly_measurement_2026-08-11.md`.

**Что из перенесённого остаётся в силе как факт, а не как вопрос:** права `PUBLIC` на месте и
запись под нашей учёткой физически возможна; формулировка «минимум ещё на 98» была шире замера и
исправлена в состоянии и в инфра-фактах тем же решением. Открытым остаётся только факт со стороны
вендора — строка `Q-15`.

## Перенесено 2026-08-11 (решение ADR-033) — строка вопроса Q-16 и блокер задачи T-0-5

Вопрос закрыт решением, а не отсрочкой: вердикт «контроль опровергнут» снят как не следующий из
предъявленного лога, контроль записи перестроен, замена назначена. Строки перенесены ДОСЛОВНО, как
требует `ADR-019`.

Строка индекса из `07_STATE.md`:

| Q-16 | OPEN | Компенсирующий контроль `ADR-032` (транзакция только на чтение) опровергнут замером под `LOMBARD_RO` — чем контроль записи заменяется | пересмотр `ADR-032` архитектором |

Полная строка из `07_GAPS.md`:

| Q-16 | OPEN | Компенсирующий контроль `ADR-032` («транзакция только на чтение на клиентской стороне держит read-only») измерен шагом 6-е задачи `T-0-5` и опровергнут — `UPDATE` под `LOMBARD_RO` в режиме `SET TRANSACTION READ ONLY` прошёл без ошибки прав. Чем контроль записи заменяется, решением не назначено | Решение `ADR-032` (не отзывать гранты `PUBLIC`) стояло на этом контроле как на единственной защите красной линии `00 §4` со стороны нашего слоя, раз она не обеспечена СУБД. Опровержение — прямо названный в поправке брифа триггер пересмотра, отзыв прав `PUBLIC` этой находкой не разрешён и не исполнялся | архитекторская сессия — пересмотр `ADR-032`; замер целиком `reference/T-0-5_readonly_tx_measurement_2026-08-11.md` | опровергнутая предпосылка решения |

Блокер, перенесённый тем же коммитом:

- **`T-0-5` заблокирована пересмотром `ADR-032` (`Q-16`).** Компенсирующий контроль, на котором
  стояло решение («транзакция только на чтение на нашей стороне держит read-only») измерен и
  опровергнут: под `LOMBARD_RO` `UPDATE` в режиме `SET TRANSACTION READ ONLY` прошёл без ошибки.
  Снимает блокер только решение архитектора — исполнитель не назначает замену контроля. До решения
  учётка `LOMBARD_RO` не используется для операций, полагающихся на read-only режим транзакции как
  на защиту. Замер — `reference/T-0-5_readonly_tx_measurement_2026-08-11.md`.

**Что из перенесённого остаётся в силе как факт, а не как вопрос:** проба шага 6-е предъявлена и
переисполнению в прежней форме не подлежит; действие исполнителя (остановка, невыпуск шага 6-ж,
вывод учётной записи из использования) признано верным при любом объяснении тишины в логе. Открытым
остаётся уровень отказа в движке — строка `Q-17`, меряется задачей `T-0-17` на нашем экземпляре.

## Перенесено 2026-08-11 (решение ADR-034) — задача T-0-12 и строка вопроса Q-15

Закрыто ОТКАЗОМ владельца, а не исполнением и не отсрочкой: письмо вендору не отправляется, вендор
доработок под нас делать не будет. Строки перенесены ДОСЛОВНО, как требует `ADR-019`.

Строка задачи из `04_ROADMAP.md` вместе с приёмкой:

| T-0-12 | Письмо вендору: уточнения по семантике начислений **и по грантам PUBLIC** (ADR-032) | письмо отправлено и содержит поимённый вопрос: полагается ли приложение PawnShop на гранты PUBLIC и допускает ли вендор их сужение до чтения; ответ получен и зафиксирован решением или строкой реестра | T-0-3 |

Строка таблицы мандата из `07_STATE.md`:

| T-0-12 письмо вендору | todo | A |

Строка индекса вопросов из `07_STATE.md`:

| Q-15 | OPEN | Полагается ли приложение PawnShop на гранты псевдо-пользователя `PUBLIC` — то есть можно ли сузить их до чтения, не остановив ERP | письменный ответ вендора (`T-0-12`) |

Полная строка из `07_GAPS.md`:

| Q-15 | OPEN | Полагается ли приложение PawnShop на гранты псевдо-пользователя `PUBLIC`, то есть возможно ли сузить их до чтения, не остановив работу ERP | Единственный факт, способный перевернуть `ADR-032`: если приложение на `PUBLIC` не полагается, отзыв становится дешёвым, красная линия `00 §4` обеспечивается на уровне СУБД, а не только нашим слоем, и компенсирующий контроль из шага 6-е задачи `T-0-5` перестаёт быть единственной защитой. Проверить у себя нельзя: копии базы нет, схема вендором не документируется, а проверка на живой базе клиента есть проверка на проде | письменный ответ вендора «Алгоритм» по задаче `T-0-12`; до ответа отзыв не исполняется ни в каком объёме | недостающий факт |

**Условие оживления названо (иначе отказ становится вечным молча):** появилась возможность задать
вопрос бесплатно — плановое обращение в поддержку по другому поводу, визит инженера вендора — ЛИБО
появился способ проверить зависимость приложения от `PUBLIC` без вендора, то есть тестовая копия
базы, на которой отзыв обратим.

**Что из перенесённого остаётся в силе как факт, а не как задача:** отзыв грантов `PUBLIC` уходит с
горизонта проекта — не «отложен до ответа», а снят, ответа не будет; запрет `REVOKE` без нового
решения владельца этим усилен, а не ослаблен. Слой СУБД в контроле записи (`ADR-033`) выбывает
навсегда, и наш слой становится единственным постоянным. Два груза письма нашли себе место:
семантика начислений закрыта внешним эталоном `00 §3-БИС(b)` и приёмкой `T-0-3b`, версия билда
PawnShop для `T-0-10` переведена на замер на сервере.

## Перенесено 2026-08-07 — закрытые задачи Фазы 0

| Задача | Статус |
|---|---|
| T-0-1 | done |
| T-0-2 | done |
| T-0-3 | done |

## Перенесено 2026-08-11 (сессия исполнителя, решение `ADR-036`) — задача `T-0-5` закрыта

Статус ставит исполнительская сессия по своему session-блоку — решение `ADR-036` явно оставило это
на её усмотрение, коротко указав: «Статус `T-0-5` ставит исполнительская сессия по своему
session-блоку, когда артефакт лежит в репозитории; класс мандата задачи остаётся B». Артефакт
`reference/T-0-5_readonly_mode_measurement_2026-08-11.md` в репозитории — оба лога 6-е-БИС и 6-ж
предъявлены и засчитаны.

Приёмка по ПОПРАВКЕ 2 брифа закрыта целиком: пройдены шаги 1-БИС, 2, 3 (заменён), 4, 6-а, 6-б, 6-д
(замеры 2026-08-08 и 2026-08-11, `reference/T-0-5_readonly_measurement_2026-08-11.md`) плюс 6-е-БИС
и 6-ж (`reference/T-0-5_readonly_mode_measurement_2026-08-11.md`). Учётная запись `LOMBARD_RO`
существует, пароль сброшен административно, права выданы скриптом `scripts/grant_ro.sql` на девяти
таблицах списка брифа.

**Что из перенесённого остаётся в силе как факт, а не как задача.** Переезд обращений к БД в класс A
закрытием `T-0-5` **не наступает** — условие (`ADR-033`) требует трёх фактов разом: замера `T-0-17`
на собственном экземпляре Firebird (не сделан, задача в очереди), `MON$`-строки о режиме транзакции
на базе клиента (предъявлена этим закрытием), обращения через единственную точку доступа с белым
списком операторов (не заведена). Принятый постоянный риск — запись в базу под чужими правами
`PUBLIC` кем угодно с паролем `LOMBARD_RO` и доступом к порту (`ADR-034`) — действует независимо
от статуса задачи и переносится в состояние отдельной строкой блокеров.

| T-0-5 | done |

## Перенесено 2026-08-12 (решение `ADR-037`) — `Q-17` закрыта замером, задача `T-0-17` исполнена

Строка реестра `07_GAPS.md` дословно, как она стояла на коммите `c91f875`:

| Q-17 | OPEN | На каком уровне Firebird 2.5.9 печатает отказ по режиму транзакции — оператора или изменяемой записи, — и отбивает ли `SET TRANSACTION READ ONLY` попытку изменения вообще | От этого зависит, есть ли у нас эшелон в движке или единственным контролем остаётся белый список операторов в нашем слое (`ADR-033`). Проба шага 6-е с предикатом `WHERE 1 = 0` различить не может: при отказе на уровне записи она до него не доходит никогда. На базе клиента решающая проба запрещена красной линией `00 §4` — изменение, задевающее строку, и есть запись в базу вендора. Меряется у нас, на выброшенной базе | задача `T-0-17` — замер на собственном экземпляре Firebird 2.5.9, класс A; побочно шаг 6-е-БИС задачи `T-0-5` снимает третье объяснение (режим не вступил в силу) | недостающий факт |

Строка индекса вопросов `07_STATE.md` дословно:

| Q-17 | OPEN | На каком уровне Firebird 2.5.9 печатает отказ по режиму транзакции — оператора или записи — и отбивает ли `SET TRANSACTION READ ONLY` изменение вообще | задача `T-0-17`: замер на собственном экземпляре |

Строка очереди исполнения `07_STATE.md` дословно:

| 1 | `T-0-17` замер `READ ONLY` на нашем экземпляре | A | **готов**: `briefs/T-0-17.md` (2026-08-11), площадка — контейнер с Firebird 2.5.9 на машине Ilyas | После отказа от письма (`ADR-034`) слой СУБД выбыл навсегда, и это единственный оставшийся источник второго рубежа. Отвечает на `Q-17` и даёт один из фактов условия переезда мандата; исполняется без сервера клиента и без подтверждений |

Строка блокеров `07_STATE.md` дословно, в той форме, в какой она перестала быть верной:

- **Эшелона в движке пока нет как подтверждённого факта.** `MON$`-строка шага 6-е-БИС предъявлена
  (режим транзакции переключается под `LOMBARD_RO`), но до предъявления замера `T-0-17` и единственной
  точки доступа с белым списком учётка `LOMBARD_RO` не используется в операциях, где режим транзакции
  только на чтение считался бы защитой. Действующая защита одна и она наша: на сервер не уходит
  ничего, кроме чтения, и файл запроса печатается до запуска.

**Ответ, которым строка закрыта.** Замер на собственном экземпляре Firebird 2.5.9 (артефакт
`reference/T-0-17_readonly_engine_measurement_2026-08-12.md`, контура клиента не касался ни одной
командой): проба с предикатом `WHERE 1 = 0` в транзакции только на чтение прошла молча при
`RO_FLAG = 1`, проба с предикатом на одну строку получила отказ `SQLSTATE = 42000`,
`attempted update during read-only transaction`, и строка осталась нетронутой. **Отказ приходит на
уровне изменяемой ЗАПИСИ, не на уровне оператора; второй рубеж существует.** Тишина шага 6-е задачи
`T-0-5` объяснена формой пробы — она физически не могла дойти до отказа. Приёмка сверена по логам
решением `ADR-037`, там же названы два расхождения формы (предъявлена версия клиента `isql` той же
инсталляции вместо строки версии сервера; тишина шага 7 зафиксирована прозой, а не дословным блоком
всего стандартного вывода) — вердикта они не касаются.

**Что из перенесённого остаётся в силе как факт, а не как задача.** Переезд чтения БД в класс A
**не наступает**: из трёх фактов `ADR-033` предъявлены два — замер `T-0-17` и `MON$`-строка с базы
клиента; третий, единственная точка доступа с белым списком операторов, не заведён. Обращения к БД
PawnShop остаются классом B целиком. Задача на недостающий факт этим закрытием НЕ заводится —
`ADR-037` называет это отдельным решением владельца. Принятый постоянный риск (`ADR-034`) замером не
уменьшен: второй рубеж защищает от нашей ошибки, а не от стороннего клиента, который транзакцию
только на чтение просто не откроет.

| T-0-17 | done |

## Перенесено 2026-08-12 (решение `ADR-042`) — `Q-7` и `Q-8` закрыты замером портфеля

Строки реестра дословно, как стояли в `07_GAPS.md` до закрытия:

| ID | Статус | Вопрос | Почему важно | Гейт | Тип |
|---|---|---|---|---|---|
| Q-7 | OPEN | Размер активного портфеля: 98 (владелец) против ~82 (конституция §5) | При подтверждении 98 конституция правится через решение | T-0-3b | недостающий факт |
| Q-8 | OPEN | Совпадёт ли число открытых договоров с ненулевым хранением с числом 6 | Набор мал и сверяется целиком, не выборкой (ADR-010) | T-0-3b | недостающий факт |

Строки индекса вопросов дословно, как стояли в `07_STATE.md`:

| ID | Статус | Вопрос одной фразой | Гейт |
|---|---|---|---|
| Q-7 | OPEN | Размер активного портфеля: 98 против 82 | T-0-3b |
| Q-8 | OPEN | Совпадёт ли число договоров с ненулевым хранением с числом 6 | T-0-3b |

**Чем закрыты.** Замер 2026-08-12 под учётной записью только на чтение
(`reference/T-0-3b_accrual_params_measurement_2026-08-12.md` §2 и §3): активных договоров `98`
(при `2321` закрытых и `73` реализованных, сумма сходится с общим счётчиком того же прогона) —
совпало с контрольным числом владельца, цифра аудита «~82» замером не подтвердилась. Договоров с
начислением за хранение — `6` по признаку `STORAGE_TYPE = 3`, совпало с контрольным счётом
владельца; первый фильтр по ненулевой ставке давал `5`, разница объяснена договором с нулевой
ставкой при заданном типе тарифа, а не подгонкой фильтра под ожидаемое число.

**Что из закрытого остаётся в силе как факт, а не как вопрос.** Закрытие `Q-8` означает, что
определено НАСЕЛЕНИЕ договоров с хранением, и только это: формула начисления хранения — предмет
`Q-9`, а он ждёт `Q-18` (`ADR-040`) вместе со всей формулой баланса. Закрытие `Q-7` повлекло правку
конституции `00 §5` и снятие числа портфеля из приёмок задач `T-0-6` и `T-2-2`: число замеряется на
дату прогона, константой в документ не вписывается (`ADR-042`, сплошной поиск —
`reference/ADJ_Q-7_portfolio_size_sweep_2026-08-12.md`).

## Перенесено 2026-08-12 (решение `ADR-046`) — `Q-2` и `Q-3` закрыты решением владельца

Регион всех облачных ресурсов — `europe-west3` (Франкфурт). Канал прод-подключения — агент на
сервере клиента (вариант Б `01 §4`), только исходящий трафик. Обе строки закрыты не замером и не
отказом, а решением владельца по существу; названный риск канала (исходящий HTTPS из сети клиента
может быть закрыт) проверяется первым шагом `T-0-8` и при провале возвращает вариант А новым
решением.

Строки реестра дословно:

| ID | Статус | Вопрос | Почему важно | Гейт | Тип |
|---|---|---|---|---|---|
| Q-2 | OPEN | Регион для всех облачных ресурсов | Задаётся один раз и меняется переездом | T-0-7 | нерешённое решение |
| Q-3 | OPEN | Канал прод-подключения: туннель из облака или агент на сервере | Производитель за структурной границей; потребители от выбора не зависят | T-0-8 | нерешённое решение |

Строки индекса состояния дословно:

| ID | Статус | Вопрос одной фразой | Гейт |
|---|---|---|---|
| Q-2 | OPEN | Регион облачных ресурсов | T-0-7 |
| Q-3 | OPEN | Канал прод-подключения к базе | T-0-8 |

Прежняя приёмка `T-0-8` дословно (задача переопределена, приёмка выбора исчерпана): «ADR: канал
прод-подключения (WG-туннель из GCP vs агент на сервере) | ADR accepted | T-0-1..0-3, пробы».
## Перенесено 2026-08-13 (решение `ADR-050`) — `Q-21` закрыта выбором механизма и принятым риском

Вопрос закрыт `ADR-050`: механизм учётных данных агента — самоподписанный OIDC плюс федерация
удостоверений (вариант 2, реализация 2b); JWKS передаётся провайдеру напрямую флагом
`--jwk-json-path`, новый публичный бакет не заводится (замер
`reference/T-0-8_jwks_delivery_measurement_2026-08-13.md`). Остаточный риск — «долгоживущий
закрытый ключ на сервере клиента при компрометации машины» — принят явно, с названной ценой
(`ADR-050` п. 4) и с назначенной ротацией раз в 90 дней (п. 8).

Строка реестра дословно:

| ID | Статус | Вопрос | Почему важно | Гейт | Тип |
|---|---|---|---|---|---|
| Q-21 | OPEN | **Каким механизмом агент на сервере клиента получает учётные данные GCP, если создание ключа сервисного аккаунта запрещено политикой организации?** Замеры 2026-08-13 (`ADR-049`): `keys create` -> `FAILED_PRECONDITION`, `constraints/iam.disableServiceAccountKeyCreation` действует (`--effective` печатает `enforce: true`), явной политики на проекте нет, попытка переопределить её на проекте -> `IAM_PERMISSION_DENIED` на `orgpolicy.policies.create`; проект лежит под организацией `320374923197`, видимости которой у Ilyas нет вовсе при роли `owner` на проекте. Живых вариантов три: **(1)** исключение или снятие ограничения администратором организации клиента; **(2)** федерация удостоверений вместо статического ключа — требует поставщика удостоверений в контуре клиента, факта о котором в репозитории нет ни одного; **(3)** возврат варианта А (туннель, pull-коннектор из GCP) — там учётные данные на сервере клиента не нужны вовсе, задание Cloud Run работает под своей служебной личностью без ключа. **Вариант 3 оценён ОТДЕЛЬНО ревью архитектора 2026-08-13 по прямому запросу владельца; четыре факта из репозитория, ни один не из памяти.** (а) **Расчёт `ADR-046` блокером не опровергнут:** из четырёх его оснований первое («агент не требует входящих соединений» при белом, возможно динамическом адресе и терминации туннеля на роутере) действует дословно, второе (структурная граница `00 §3-БИС(a)`) нейтрально к выбору, третье (у точки доступа нет другого дома) ослаблено — дом у неё теперь есть и он переносим, четвёртое (регион) к каналу не относится; названный риск `ADR-046` («агент не достучится до GCS») закрыт замером (`ADR-048`), а запрет ключа SA в расчёт того решения не входил вовсе. Итог: ветка агента подорожала на неучтённую статью, ветка туннеля от этого не подешевела. (б) **Сделанная работа при развороте теряется частично и меньше ожидаемого:** `connector/agent/access_point.py` (305 строк), `connector/agent/test_access_point.py` (308) и `connector/agent/schema_guard.py` (143) канала не касаются вовсе — подключение приходит параметром, транзакция подменяема, обращений к GCS в них нет; отпадает `connector/agent/gcs_upload.py` (154 строки), `connector/agent/agent.py` (150) переезжает частично (список таблиц, чтение, сериализация — остаются; выгрузка с машины клиента — нет). Третий факт условия переезда чтения БД в класс A (`ADR-033`) при развороте СОХРАНЯЕТСЯ. (в) **Сама ветка туннеля не измерена ни одним артефактом:** строки WG в `11_INFRA_FACTS` (доступ извне, peer `lombard-connector`, правило файрвола на VPN-подсеть) — стартовая заливка `7b11404` от 2026-07-10; перемер `git log -L 33,34:11_INFRA_FACTS.md` от 2026-08-13 печатает ровно один коммит, тот же. Живым туннель предъявлялся только для peer'а Ilyas и только по RDP (`07_ARCHIVE`, раздел `METHOD-UPGRADE`); peer `lombard-connector` не предъявлялся handshake'ем ни разу. Вдобавок WG-подсеть `172.168.88.0/24` — публичный диапазон (`ADR-008`, техдолг, отложенный на `T-4-4` со словами «работу сейчас не блокирует»): при туннеле из GCP он перестаёт быть отложенным и становится частью работы. (г) **Ветка туннеля стоит под тем же классом риска, что убил шаг 5:** политики организации `320374923197`, действующие на этом проекте, не перечислены ни разу, и то, что нужно туннелю (внешний адрес, сетевые ресурсы, место терминации WG-клиента в GCP), измерено ровно так же — никак. **Вывод ревью:** разворот сейчас есть обмен измеренной цены на неизмеренный риск; варианты 1 и 2 бюджета наших сессий не потребляют, вариант 3 потребляет немедленно — оценка стоимости разворота не меньше трёх-четырёх сессий (новое решение владельца, переоформление задачи, замер handshake на роутере клиента, подъём WG-клиента в GCP, переезд кода, прогон) при остатке бюджета дискавери 2 сессии и 16 объявленных сессиях на 18 дней. **Рекомендация архитектора: сейчас не разворачивать**; вариант 3 держится веткой, включаемой по названному триггеру, а не сравнением на глаз. Отклонены с названной причиной: ключ в чужом проекте (`00 §4`), пользовательские креды на сервере клиента, самодельная схема выдачи подписанных URL (`ADR-048`), подписанные URL с ручным продлением (пункт 1 контракта готовности) **Развилка ВНУТРИ варианта 2 разобрана архитектором 2026-08-13; ни одного нового факта о контуре клиента не добавлено — добавлены разбор механизмов, названный ограничитель и состав замера.** Источником федерации Google принимает: AWS (у клиента его нет — вне рассмотрения), OIDC-поставщика, SAML-поставщика и сертификатный вариант (последний — внешнее знание, не измерено; для него нужен собственный удостоверяющий центр, то есть та же пара ключей плюс лишний слой). Факта о корпоративном поставщике (AD FS, Azure AD/Entra) в репозитории нет НИ ОДНОГО, и предполагать его наличие запрещено. Механизмов на столе поэтому два. **(2a) Существующая корпоративная IdP клиента:** существование не подтверждено; при наличии требует участия IT клиента, публично достижимого метаданного эндпоинта и согласия администратора — то есть той же внешней зависимости, что и вариант 1. **(2b) Самоподписанный OIDC:** пара ключей генерируется НА сервере ERP, агент сам подписывает JWT, обмен на кратковременный токен идёт через `sts.googleapis.com`, доступ сужается `audience` пула и `attribute-condition` провайдера до единственного сервисного аккаунта `lombard-pipeline@` с уже выданными ему ролями. **2b не зависит от IT-инфраструктуры клиента вовсе — весь контроль на нашей стороне.** Честная оценка риска, обе половины: 2b **НЕ снимает** класс риска «долгоживущий закрытый ключ лежит на сервере клиента» — при компрометации машины эффективный доступ тот же, что дал бы запрещённый ключ SA; снимает он три ДРУГИХ — ключ не является учётными данными Google (ограничение организации исполняется по букве, а не обходится: ни один запрещённый объект не создаётся), отзыв целиком в наших руках и мгновенен (удаление провайдера или ротация JWKS вместо удаления ключа SA, которого мы создать не можем), радиус ограничен условием провайдера и ролями одного сервисного аккаунта. **Красные линии `00 §4`:** БД не затрагивается вовсе (ключ не является кредом базы и режим чтения не меняет); ресурсы федерации создаются в проекте клиента; закрытый ключ есть **НОВЫЙ секрет** — место хранения только файл на сервере ERP с правами на учётную запись службы агента, в репозиторий, в артефакты и в логи он не попадает никогда, ротация — генерацией новой пары с временным перекрытием двух открытых ключей в JWKS, периодичность назначается решением владельца при выборе пути, а не молча. **Ограничитель, найденный фактом репозитория:** «опубликовать JWKS в существующем бакете `T-0-7`» неисполнимо — все три бакета созданы с `public_access_prevention: enforced` (`11_INFRA_FACTS`), а issuer-документ обязан читаться анонимно. Значит либо JWKS передаётся провайдеру напрямую при его создании (это внешнее знание о платформе, то есть гипотеза `ADR-045`; проверяется печатью `--help` команды создания OIDC-провайдера — вывод справки, ни одного объекта не касается), либо заводится НОВЫЙ публичный бакет — отдельное решение владельца, потому что публичный объект в контуре клиента заводится не сессией. **Второй ограничитель, не измеренный ни разу:** федерация ходит на `sts.googleapis.com` и `iamcredentials.googleapis.com`, а шаг 1 мерил только `storage`, `oauth2` и `secretmanager` — связность к этим двум входит в тот же замер. **Рекомендация архитектора: мерить под 2b, а 2a ловить попутно.** Отдельная карточка на гипотетический AD FS стоит сессию и раунд с владельцем при остатке бюджета дискавери 2 сессии и 18 днях до даты поставки; три read-only команды внутри уже нужного скрипта не стоят ничего, и если корпоративная IdP всё-таки есть — она обнаружится тем же прогоном. **Состав замера на сервере (класс B, только чтение, ОДНА карточка, скрипт `scripts/probe_wif_identity.ps1`):** домен или рабочая группа и роль машины; роли и службы AD FS / AD DS / Azure AD Connect плюс установленное ПО по признакам федерации с обратным контролем; версия PowerShell и .NET и фактическая способность подписать и проверить RS256 в памяти (ключ не сохраняется: `PersistKeyInCsp = $false`, `Clear()`); наличие и версия Python; расхождение часов (`w32tm`); связность к `sts.` и `iamcredentials.` под штатным валидатором (`ADR-048`). Замер ничего не устанавливает, ни одного файла не создаёт, к БД не обращается и секретов не печатает; откат предмета не имеет. | Без учётных данных агент не выкладывает выгрузку в GCS: канал не замыкается и конвейер не идёт сам. Это единственный открытый вопрос, стоящий прямо на пункте 1 контракта готовности `00 §6` при 18 днях до даты поставки | Ответ владельца по развилке (`ADR-049` п. 7) плюс замер 5-БИС (`briefs/T-0-8.md`, ПОПРАВКА 2) — read-only, класс B, одна карточка; он сужает развилку до вопроса клиенту. **Ревью 2026-08-13 развилку не сузило и решения не приняло** — оно оценило вариант 3 отдельно (текст в колонке вопроса) и назвало, чего для него не хватает: живого handshake peer'а `lombard-connector` и перечня политик организации, действующих на проекте. Расширение состава замера 5-БИС на политики, относящиеся к ветке туннеля, предложено сессией как патч брифа и без апрува владельца не применено. **Замер 5-БИС базового состава исполнен 2026-08-13** (`reference/T-0-8_org_policy_measurement_2026-08-13.md`): `iam.workloadIdentityPoolProviders` — `allowAll: true`, федерация организацией НЕ запрещена, пул федерации в проекте ещё не заведён. **Вариант 2 остаётся живым**, развилка НЕ сужена до вопроса клиенту — она сужена до недостающего факта на стороне контура клиента: есть ли на сервере ERP (Windows Server) пригодный поставщик идентичности для workload identity federation (механизм — OIDC/SAML/иной — не определён, это отдельный замер на сервере, ещё не выполнявшийся). Замер политик ветки туннеля (пункт «г» ревью) по-прежнему не исполнен. **Недостающий факт теперь один и он на сервере клиента.** Разбор развилки внутри варианта 2 сделан 2026-08-13, скрипт замера готов — `scripts/probe_wif_identity.ps1`, класс B, только чтение, одна карточка; текст карточки и ПОПРАВКА 3 брифа `T-0-8` предложены сессией и без апрува владельца НЕ применены. Проверка платформенной гипотезы о передаче JWKS провайдеру напрямую — печать справки команды создания OIDC-провайдера, объектов не касается. **Замер сервера ERP исполнен Ilyas 2026-08-13** (`reference/T-0-8_server_identity_measurement_2026-08-13.md`): сервер — рядовой сервер в рабочей группе, НЕ в домене; корпоративного поставщика идентичности НЕТ, подтверждено тремя независимыми способами (роли AD FS/AD DS/WIF — `Available`, то есть не установлены; семь служб AD FS/AD DS/Azure AD Connect — все «НЕ ЗАРЕГИСТРИРОВАНА»; поиск по установленному ПО — 0 совпадений при обратном контроле в 39 записей). Путь **2a (существующая корпоративная IdP) закрыт фактом.** Путь **2b (самоподписанный OIDC) криптографически годен**: RSA 2048, подпись и проверка RS256 в памяти получены, открытая часть экспортируема для JWKS. Связность к `sts.googleapis.com` и `iamcredentials.googleapis.com` подтверждена — оба `HTTP 404` под штатным валидатором (`ADR-048`, тот же паттерн, что закрыл остаток `secretmanager`). Дополнительно: Python на сервере не установлен (не блокер механизма, но зависимость для будущего шага 6 — установки агента); служба времени Windows (`w32tm`) не запущена (`0x80070426`) — риск дрейфа часов для JWT, учесть перед боевым прогоном. **Развилка ВНУТРИ варианта 2 закрыта: единственный доступный механизм — 2b, самоподписанный OIDC.** Остаётся нерешённым и держит `Q-21` в статусе OPEN: (а) место публикации JWKS — новый публичный бакет (решение владельца, все три бакета `T-0-7` закрыты от публичного доступа) либо передача провайдеру напрямую (платформенная гипотеза, не проверена — печать `--help`, объектов не касается); (б) подтверждение владельца на выбор самого пути федерации как механизма `Q-21` (риск-анализ дан ревью архитектора 2026-08-13 — 2b не снимает риск компрометации машины, но снимает три других названных риска). | **Радиус поражения:** шаги 5–7 `T-0-8` и через них пункты 1–3 контракта готовности (конвейер, светофор, алерты). НЕ блокирует: `T-0-4` и `T-0-3b` (они ходят к базе вручную по карточкам, канал им не нужен), форму осмотра, веб-приложение и всё, что живёт целиком в облаке — там служебная личность привязана к ресурсу и ключа не требует. Шаги 1–4 и 8 `T-0-8` предъявлены и вопросом не отменяются | нерешённое решение + недостающий факт (администратор организации) |

Строка индекса состояния дословно:

| ID | Статус | Вопрос одной фразой | Гейт |
|---|---|---|---|
| Q-21 | OPEN | Механизм учётных данных агента: ключ SA запрещён политикой организации клиента — исключение, федерация или возврат к туннелю | ответ владельца + замер 5-БИС `T-0-8` |

--- END 07_ARCHIVE.md ---
