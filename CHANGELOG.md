# CHANGELOG

## [2026-07-14] T-0-2: DB structure extraction complete

**SESSION LOG · 2026-07-14 · T-0-2**

- **Задача:** T-0-2 — Снятие полной структуры БД PawnShop
- **Сделано:** найден рабочий .fdb (D:\PawnShop_DOL\DB\DOL.FDB, подтверждён строкой подключения клиента PAWN.DB); подтверждены креды SYSDBA/masterkey (дефолт Firebird подошёл); снята структура через isql (108 таблиц, 1018 колонок, кросс-проверка 100%); дамп передан архитектору в чат
- **Команды/логи ключевые:** Get-ChildItem *.fdb по дискам → db.ini/pawn.ini → isql SHOW DATABASE (успех) → meta.sql → meta_out.txt (1 773 072 байт)
- **Отклонения от плана:** BOM в meta.sql вызвал безобидную ошибку на первой строке (комментарий), оба SELECT выполнились нормально; кракозябры при чтении Get-Content без -Encoding — не баг файла, чтение с явным UTF8 показало корректную кириллицу

## [2026-07-14] T-0-2: DB structure extraction complete

**SESSION LOG · 2026-07-14 · T-0-2**

- **Задача:** T-0-2 — Снятие полной структуры БД PawnShop
- **Сделано:** найден рабочий .fdb (D:\PawnShop_DOL\DB\DOL.FDB, подтверждён строкой подключения клиента PAWN.DB); подтверждены креды SYSDBA/masterkey (дефолт Firebird подошёл); снята структура через isql (108 таблиц, 1018 колонок, кросс-проверка 100%); дамп передан архитектору в чат
- **Команды/логи ключевые:** Get-ChildItem *.fdb по дискам → db.ini/pawn.ini → isql SHOW DATABASE (успех) → meta.sql → meta_out.txt (1 773 072 байт)
- **Отклонения от плана:** BOM в meta.sql вызвал безобидную ошибку на первой строке (комментарий), оба SELECT выполнились нормально; кракозябры при чтении Get-Content без -Encoding — не баг файла, чтение с явным UTF8 показало корректную кириллицу

## [2026-07-14] T-0-3: Маппинг таблиц PawnShop → canonical

**SESSION LOG · 2026-07-14 · T-0-3**

- **Задача:** T-0-3 — Маппинг таблиц PawnShop → canonical
- **Сделано:** принят дамп T-0-2 (108 таблиц/1018 колонок, сверено); шорт-лист кандидатов по 02 §3; серия confirm-SELECT'ов на живых данных (открытые и закрытые авто-договоры); найден и подтверждён источник VIN/госномера/модели/года через механизм custom fields (CUSTOM_FIELDS_VALUES + DIR_CUSTOM_FIELDS, DOC_CODE вида \MSUBJS:S_AUTO_*); подтверждены rate/penalty на реальных значениях; выявлено неучтённое в 03§4 поле STORAGE_VAL; заполнена 02 §3; собран mapping.json (драфт)
- **Команды/логи ключевые:** meta_dump T-0-2 (принят как есть); confirm_t03.sql (4 запроса); confirm_t03b.sql (пеня/хранение/страховка на открытых договорах); diag_customfields.sql (раскрыл реальный DOC_CODE вместо локализационного ключа NAME); confirm_vin2.sql (VIN подтверждён на 5 живых договорах 2025-2026)
- **Отклонения от плана:** да — шаг 2 брифа (свежий RDB$-эталон) фактически не выполнен отдельно, вместо него использован T-0-2-дамп как структурная база + сверен по факту через confirm-SELECT'ы (структура совпала); путаница с DOC_CODE (взял NAME вместо DOC_CODE) потребовала доп. диагностического запроса
