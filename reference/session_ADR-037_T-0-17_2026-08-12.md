# Session-блок · 2026-08-12 · приёмка `T-0-17`, закрытие `Q-17`, решение `ADR-037`

=== SESSION LOG · 2026-08-12 · T-0-17 (приёмка) ===

## SESSION_LOG
- Задача: `T-0-17` — приёмка замера `SET TRANSACTION READ ONLY` на собственном Firebird 2.5.9 и
  закрытие вопроса `Q-17` решением; сама задача исполнена и закоммичена предыдущей сессией.
- Роль и модель сессии: архитектор · opus (`model` в `.claude/agents/architect.md`; `ADR-030`)
- Коммит на входе: `c91f875629af4b05026f0eeaab03e4940979cff8` (`git rev-parse HEAD`)
- Сделано:
  - Прочитан целиком артефакт `reference/T-0-17_readonly_engine_measurement_2026-08-12.md` и бриф
    `briefs/T-0-17.md`; каждый критерий приёмки сверен с логом, а не с вердиктом исполнителя.
  - Приёмка засчитана; два расхождения формы названы поимённо (версия клиента вместо версии
    сервера; тишина шага 7 прозой вместо дословного блока стандартного вывода).
  - Принято решение `ADR-037`: вердикт «отказ на уровне ЗАПИСИ», эшелон в движке подтверждён,
    `Q-17` закрыта, переезд чтения БД в класс A НЕ наступает и задач на недостающий факт решение
    не создаёт.
  - Прогнан сплошной поиск понятия (`ADR-027`): 91 совпадение на коммите `c91f875`, по каждому
    файлу принято решение «правится» / «остаётся» — в теле решения.
  - Применены патчи: состояние, реестр вопросов, архив, оглавление, роудмап, контракты данных,
    инфра-факты. Коммит `b8311ad`, хук: «пройдено, нарушений нет» (7 проверок, срабатываний 0).
- Команды/логи ключевые:
  - `tools/session_status.sh` → счётчики 1–4 нулевые, счётчик 5 = 2 (впереди `origin/main` на два
    коммита, отставание 0), вердикт «НЕ ЧИСТО» — разобран ниже как известная развилка.
  - `tools/hooks/selftest.sh` → «кейсов проверено: 41 · провалено 0»; симлинк
    `.git/hooks/pre-commit → ../../tools/hooks/pre-commit` на месте.
  - Сплошной поиск: `grep -rnE "T-0-17|Q-17|эшелон|второй рубеж|не измерен"` по `*.md`, `*.sql`,
    `*.sh`, `*.json` — 91 строка, перечень по файлам в решении.
- Отклонения от плана: одно, названное. Текст запуска сессии ожидал `INFRA_PATCH` «нет изменений»;
  фактически строка учётной записи в реестре инфра-фактов несла утверждение «уровень отказа НЕ
  измерен», ставшее неверным. Ресурсы контура клиента не изменились ни одним битом, изменилась
  запись факта — патч ниже не пустой и это названо явно.

## STATE_PATCH  (→ 07_STATE.md)
- Задача `T-0-17`: todo → done. Вопрос `Q-17`: OPEN → закрыт фактом, перенесён в архив дословно.
- Стенд-ап (ЗАМЕНЯЕТ блок целиком, ровно пять строк):
  - Прошлый шаг: `T-0-17` исполнена и принята по логам — артефакт
    `reference/T-0-17_readonly_engine_measurement_2026-08-12.md`, `Q-17` закрыт решением `ADR-037`.
  - Где мы: уровень отказа в движке измерен — режим только на чтение отбивает изменение на уровне
    задеваемой записи, второй рубеж существует; основного слоя (единственной точки доступа с белым
    списком) по-прежнему нет.
  - Следующий шаг: `T-0-3b` контракт расчёта баланса, класс B, бриф нужен.
  - Развилки на владельце: публикация коммитов наружу (`git push`); заводить ли работу по
    единственной точке доступа с белым списком операторов и когда.
  - Счётчик: фаза M — 5/5 закрыто; фаза 0 — 7 из 16 строк роудмапа закрыто.
- Подробности для модели: добавлены четыре строки — вердикт замера и рабочее следствие о пробе с
  нулевым предикатом; статус переезда мандата (два факта из трёх, задача на третий не заводится);
  два расхождения приёмки; разбор вердикта «НЕ ЧИСТО» скрипта старта. Приведены к измеренному
  строки о трёх объяснениях тишины, об условии переезда и о закрытии `T-0-5`. Счётчик свободных
  номеров: следующее решение — `ADR-038`.
- Открытые вопросы: новых нет. `Q-17` закрыт.
- Блокеры: строка об эшелоне переписана — эшелон подтверждён, блокер сузился до отсутствия
  основного слоя (единственной точки доступа с белым списком операторов). Принятый постоянный риск
  записи под правами `PUBLIC` — без изменений, замером не уменьшен.
- В архив: строка `Q-17` из реестра дословно, строка индекса вопросов дословно, строка очереди
  исполнения дословно, прежняя строка блокеров дословно, строка задачи `| T-0-17 | done |` —
  все тем же коммитом `b8311ad`.
- updated_at: 2026-08-12
- обновил: архитектор (сессия: приёмка `T-0-17`, `ADR-037`)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- Ресурсы контура клиента: **нет изменений.** Ни сервер ERP, ни база PawnShop, ни облако, ни
  файрвол, ни VPN этой работой не затрагивались; замер исполнялся в контейнере на машине Ilyas и
  площадка снесена. Собственная оснастка в реестр ресурсов клиента не попадает.
- Пользователь `LOMBARD_RO`: строка приведена к измеренному — уровень отказа по режиму транзакции
  больше не «НЕ измерен», а измерен на нашем экземпляре той же версии (отказ на уровне задеваемой
  записи, `SQLSTATE = 42000`); добавлен артефакт замера в перечень. Право самой учётной записи не
  менялось, изменилась запись факта.

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md)
- `ADR-037`: `Q-17` закрыта замером — отказ по режиму транзакции приходит на уровне ЗАПИСИ; эшелон
  в движке подтверждён; переезд чтения в класс A не наступает.
  Контекст: `ADR-033` оставил два неразличённых объяснения тишины шага 6-е; различить их на базе
  клиента запрещено структурно, проверка уехала на наш экземпляр.
  Решение: логи засчитаны, вердикт принят, `Q-17` закрыта; слой 2 назван механизмом, отбивающим
  запись, с названной границей; проба с нулевым предикатом объявлена не измеряющей режим;
  мандат остаётся классом B — предъявлены два факта из трёх; задача на третий факт решением не
  заводится.
  Последствия: перечислены в теле решения по одному пункту на файл с маркерами времени прихода.
  Статус: accepted.

## NEW_CONVENTIONS  (→ предложение правки 08_TASK_BRIEF_TEMPLATE.md)
- proposed → принято решением `ADR-037` как правило, применяется [отдельным коммитом]: **критерий
  приёмки и команда, которой он закрывается, обязаны совпадать по ПРЕДМЕТУ.** Прецедент — критерий
  `T-0-17` требовал версию сервера, а предписанная тем же брифом команда печатает версию клиента.
  Проверяется глазами того, кто бриф пишет, потому что запускает команду другой.

## ДОПОЛНЕНИЕ той же сессии — ответ владельца по развилке и публикация

Блок один (`05 §III`), поэтому ответ владельца дописан сюда, а не оформлен вторым блоком.

- **Развилка снята владельцем: вариант Б.** Единственная точка доступа с белым списком операторов
  заводится ПОСЛЕ выбора канала прод-подключения. Записано решением `ADR-038` вместе с триггером;
  задача в очередь не заводится, строки роудмапа не получает, новый вопрос в реестр не заводится.
  Мандат обращений к БД не двигается: третий факт условия переезда по-прежнему не предъявлен.
- **Публикация наружу исполнена по явному слову владельца** (класс B, разрешение разовое).
  До запуска отдельным сообщением названо: что публикуется (ветка `main` в публичный репозиторий),
  сколько коммитов и чем откатывается (только новым коммитом — публичный репозиторий изъятием не
  откатывается). Проверка на секреты пройдена хуком в каждом коммите, срабатываний 0.
- **STATE_PATCH дополнен:** развилки на владельце — «нет»; условие завода точки доступа записано в
  подробности; строка о вердикте скрипта старта переписана — публикация красный вердикт сняла, но
  причина осталась (счётчик складывает отставание и опережение в одно число). Следующий свободный
  номер решения — `ADR-039`.
- **INFRA_PATCH дополнением не затронут:** ресурсы контура клиента по-прежнему без изменений.

## ДОПОЛНЕНИЕ 2 той же сессии — выпущен бриф `T-0-3b`

- **Перед выпуском брифа закрыто решением `ADR-039` три места, где действующие решения не сходились
  друг с другом или молчали,** и каждое исполнитель встретил бы как стоп-условие: первоисточник
  оракула требовался в приватный сателлит, которого не существует; санитария имён полей вендора
  читалась в две противоположные стороны; отсутствующий `connector/mapping.json` провоцировал
  создать его мимо задачи-владельца.
- **Выпущен `briefs/T-0-3b.md`** — восемь шагов, класс B целиком, четыре шага идут на сервер клиента
  и несут отдельные карточки подтверждения. Приёмка выведена из формулы (`ADR-023`): обе формулы
  выписываются, разница предъявляется ДО расчёта ожидаемой величиной — ноль на контрольной группе
  без хранения, положительная величина на шести договорах с хранением. Оракул сверяется целиком по
  всем шести, дата расчёта совпадает с датой снимка экрана.
- **Правило вердикта записано отдельно:** не сошёлся оракул — формула в `03 §4` не пишется вовсе,
  расхождение предъявляется числами по каждому договору, и закрывается оно правилом или решением, а
  не подгонкой нашего расчёта под цифру экрана.
- **STATE_PATCH дополнен:** очередь — бриф `T-0-3b` готов; в подробности добавлена строка о трёх
  закрытых местах и правиле вердикта; следующий свободный номер решения — `ADR-040`.
- **INFRA_PATCH:** без изменений, брифом ресурсы не затрагиваются.

=== END SESSION ===
