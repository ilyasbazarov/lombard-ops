# FILE: reference/session_METHOD-UPGRADE_2026-08-07.md

```
=== SESSION LOG · 2026-08-07 · METHOD-UPGRADE ===

## SESSION_LOG
- Задача: METHOD-UPGRADE — апгрейд метода с первого поколения до поколения донора
- Коммит на входе: 62056885600615ce3efaac90a172f90fd720872c · дерево чисто
- Сделано:
  - Прочитан обязательный контекст целиком, включая 05_CONVENTIONS; проверка подмены пройдена
    на всех 26 отслеживаемых .md — первая строка каждого равна имени файла, СОДЕРЖИМОЕ НЕ ТО
    не сработало ни разу.
  - Инвентарь дельты ПЕРЕСЧИТАН наблюдением по диску, не переписан из входного файла:
    13 отсутствующих артефактов подтверждены поимённой проверкой существования;
    ноль машиночитаемых полей брифа подтверждён сплошным поиском с обратной проверкой
    исправности поиска.
  - Семь развилок адъюдицированы против фактов репозитория: 3 приняты как есть
    (один репозиторий · модель исполнения · язык), 4 приняты с правкой
    (граница контракта · приёмка · ревью · состав класса B). Отклонений нет.
  - Найдено 8 расхождений входного файла с репозиторием; во всех победил репозиторий,
    все названы поимённо в артефакте адъюдикации.
  - Оформлено 10 решений (ADR-017 … ADR-026); девять accepted, одно proposed с названным гейтом.
  - План перехода — 6 задач роудмапа, у каждой класс мандата и проверяемая приёмка
    (лог, число, зелёный самотест либо замер на реальном содержимом репозитория).
  - Выданы 20 drop-in-патчей со сводкой «файл → операция» и 2 брифа механики в новом формате
    шапки (класс задачи · параллель · файлы на запись).
  - Три срочных пункта заведены задача-строками с гейтами.
  - ВТОРОЙ КРУГ: прочитаны оригиналы метода донора с диска по абсолютным путям — канон, законы,
    конвенции, три файла ролей, роль сборки, шаблон брифа, руководство владельца, шесть скриптов,
    четыре файла агентов, файл прав инструмента. Форма взята с оригиналов, не с пересказа.
    Ни один номер решения донора в перенос не ушёл.
  - Круг 2 изменил три вещи: (1) отказ создавать канон копией СОХРАНЁН, но основание заменено —
    канон донора прочитан и отстал от собственной практики донора в четырёх местах, донор держит
    это открытой строкой реестра; (2) заведено новое решение ADR-027, обоснованное отказом
    ЭТОЙ сессии; (3) шаблон брифа переведён с точечной вставки на полную замену по оригинальной
    форме, добавлено руководство владельца на три существующих режима.
- Команды/логи ключевые:
  - `git rev-parse HEAD` → 62056885600615ce3efaac90a172f90fd720872c
  - `git status --porcelain` → пустой вывод (дерево чисто)
  - `grep -c '^## ADR-' 06_DECISIONS_LOG.md` → 12 (входной файл утверждал 16)
  - поиск полей брифа по briefs/ и 08 → 0 совпадений; обратный запрос по слову «Приёмка»
    дал совпадения во всех пяти файлах — значит инструмент исправен, ноль есть факт
  - `git log -S'SYSDBA'` → 8 коммитов; `diff` блоков CHANGELOG строк 3–11 и 12–20 → пусто
    (запись T-0-2 продублирована целиком)
  - сверка состояния и роудмапа → 7 задач в состоянии без строки в роудмапе
  - `wc -c` → 07_STATE 4450 · 06_DECISIONS_LOG 14396 · CHANGELOG 6949
- Отклонения от плана:
  - Раздел 24 входного файла утверждал «роудмап менять не надо» — опровергнуто замером,
    заведено отдельным решением ADR-026 и шагом M-2.
  - Раздел 18(b) входного файла предлагал класть снимок эталона в публичный /reference —
    сталкивается с красной линией о персональных данных; принято с правкой (ADR-024).
  - `_METHOD.md` НЕ создан. В круге 1 — потому что на входе был только пересказ. В круге 2
    оригинал прочитан, и отказ подтверждён на более сильном основании: канон донора разошёлся
    с действующими законами донора в четырёх местах (модель исполнения · ретайренная роль ·
    режим чтения · формат блока). Импорт копией занёс бы ровно те утверждения, которые эта сессия
    отменяет. Триггер возврата — появление второго проекта на этом методе.
  - Роль сборки, буфер, скрипт проверки пересечения и инвентарь буфера НЕ заведены, хотя оригиналы
    прочитаны и форма известна: они принадлежат ОТЛОЖЕННОЙ части ADR-025 (вариант C).
  - КРУГ 3: владелец выбрал вариант C по развилке об изоляции. Решение расщеплено на принятую
    часть (закон об одном рабочем дереве на сессию) и отложенную (буфер, проход сборки, скрипт
    проверки пересечения); гейт отложенной части заострён до проверяемой формулировки.
  - Сплошной поиск понятия по репозиторию (требование ADR-027) прогнан на собственной свежей
    правке и ОКУПИЛСЯ НЕМЕДЛЕННО: первый прогон нашёл двадцать строк в пяти файлах, бо́льшая часть
    несла устаревший статус. То есть правка по названным якорям оставила остаток ровно так, как
    правило предсказывает — в той же сессии, в которой правило заведено.
  - КРУГ 4: требование владельца — все роли внутри инструмента, на ЛОКАЛЬНОМ репозитории,
    сетевого чтения нет. ADR-021 переписано с «два режима» на «один режим»; при упразднении
    закрыта дыра: разговор без доступа к диску сессией НЕ является и в репозиторий не входит,
    пока не переформулирован задачей. Руководство владельца расширено с трёх режимов до четырёх —
    добавлен режим ревью правки на границе контракта: развилка о ревью назвала его одним из трёх
    механизмов, а механизм без текста запуска не запускается никогда.
  - НАХОДКА сплошного поиска в круге 4: ОБА невыполненных брифа (T-0-5, T-0-10) несут
    незаполненный плейсхолдер коммита в разделе context-to-load. Бриф в таком виде неисполним
    в любом режиме. Это ровно те два брифа, которые состояние называет текущим фокусом.
    Перегенерация внесена в приёмку обеих задач строками роудмапа. Закрытые брифы T-0-2 и T-0-3
    помечены «без правок»: их ссылки зафиксированы по реальным коммитам и служат провенансом.
  - НАХОДКА О ФОРМЕ, стоившая двух ошибок подряд: счёт совпадений, записанный в прозу, перестаёт
    быть фактом сразу после записи. Сначала я привёл счёт, не измерив его; затем измерил — и число
    разошлось со следующей же правкой того же файла. Из артефактов убран точный счёт, вместо него
    записаны команда воспроизведения и критерий вердикта. Это тот же класс, что закон «успех
    инструмента не факт», применённый к собственному отчёту: проверяемым обязано быть не число,
    а способ его получить.
  - Развилка об иерархии источников эталона владельцу не задана: снята чтением
    (ADR-006 и ADR-010 v3 уже называют экран приложения вендора оракулом, а число владельца —
    независимым контрольным счётом).
  - Развилка о тестовом чате мессенджера владельцу не задана: предмета не существует
    (бот не создан), заведена строкой реестра с гейтом.

## STATE_PATCH  (→ 07_STATE.md)
- Задача METHOD-UPGRADE: — → done (адъюдикация закрыта, патчи и брифы выданы)
- Задачи M-1 … M-6: — → todo
- Стенд-ап (ЗАМЕНЯЕТ блок целиком, ровно пять строк):
  - Прошлый шаг: адъюдикация переноса метода закрыта, решения ADR-017…ADR-026 оформлены —
    артефакты reference/ARCH_METHOD-UPGRADE_2026-08-07.md и
    reference/PATCHES_METHOD-UPGRADE_2026-08-07.md
  - Где мы: метод первого поколения работает, механических проверок ноль; переход расписан
    шестью задачами, ни одна не начата
  - Следующий шаг: M-1 (законы и роли)
  - Развилки на владельце: апрув ADR-017…ADR-027 и коммит патчей · подтверждение на M-5 (класс B)
  - Счётчик: 0/6 задач фазы M закрыто
- Подробности для модели:
  - Порядок задач фазы M не переставляется: хук (M-4) адресует 06_INDEX.md, 07_GAPS.md,
    07_ARCHIVE.md — файлы, создаваемые M-2. Хук раньше M-2 будет краснеть на легитимном
    входе и будет отключён.
  - Следующий свободный номер решения — по факту журнала. Номера ADR-011…ADR-014 в журнале
    отсутствуют и НЕ переиспользуются.
  - Дискавери БД ожидает по решению владельца: апгрейд метода идёт первым. Текущий фокус
    T-0-5 и T-0-10 снят с позиции первого шага и переведён в очередь после фазы M.
- Открытые вопросы: девять унаследованных получают идентификаторы Q-1…Q-9 со статусом и
  гейтом; заводятся три новых — Q-10 (тестовый чат, DEFER, гейт — задача создания бота),
  Q-11 (ветка под недеплоенный патч, DEFER, гейт — первый деплой облачного ресурса),
  Q-12 (буфер и проход сборки — отложенная часть ADR-025, DEFER, гейт — две задачи класса A с
  непересекающимися наборами; закон об одном дереве на сессию в эту строку НЕ входит, он принят)
- Блокеры: раскрытие пароля администратора БД — формулировка УТОЧНЕНА В СТОРОНУ УХУДШЕНИЯ:
  значение присутствует в журнале изменений дважды, в брифе T-0-2 литералом и в 8 коммитах
  публичной истории. Удаление файлов не закрывает — история уже роздана. Закрывается только
  ротацией (M-5, класс B). Проверка на секреты в хуке закрывает повторение, но не этот случай.
- В архив: раздел «Сделано» целиком (10 строк) и закрытые задачи T-0-1, T-0-2, T-0-3 —
  переносятся в 07_ARCHIVE.md ДОСЛОВНО тем же коммитом
- updated_at: 2026-08-07
- обновил: архитектор (сессия: METHOD-UPGRADE)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md; обязателен всегда, пустой = явное "нет изменений")
- нет изменений — сессия документарная, ни один облачный, сетевой или внешний ресурс не
  создавался, не менялся и не опрашивался; репозиторий подтверждён как публичный
  (github.com/ilyasbazarov/lombard-ops), опубликованная ветка origin/main существует —
  это подтверждение уже записанного факта, не новый факт

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; полный текст — в файле патчей, П-11)
- ADR-017: Главный принцип — граница контракта двойная + закон фикс-форварда · accepted
- ADR-018: Модель исполнения: сессия исполняет сама; мандат по классу задачи · accepted
- ADR-019: Компактизация состояния: индекс, архив, реестр вопросов, стенд-ап · accepted
- ADR-020: Ретайр роли Applier · accepted
- ADR-021: Единственный режим чтения — ЛОКАЛЬНЫЙ ДИСК; единственный список обязательного
  контекста · accepted (переписано по требованию Ilyas 2026-08-07: сетевой режим не разводится,
  а упраздняется; правка законна без нового решения — черновик не выпущен в журнал)
- ADR-022: Механические проверки на коммите · accepted
- ADR-023: Три типа приёмки; правило формулы; след от проверки глазами · accepted
- ADR-024: Эталон датируется снимком; первоисточник — в приватный сателлит · accepted
- ADR-025: Изоляция параллельных сессий, буфер, проход сборки ·
  **частично accepted / частично proposed** (вариант C, решение Ilyas 2026-08-07).
  Принято сейчас: закон «одно рабочее дерево на сессию» плюс счётчики деревьев и веток в скрипте
  старта — стоит одно предложение, механика под него уже приходит задачей M-4.
  Отложено: буфер, проход сборки, скрипт проверки пересечения — предмет измерен и отсутствует
  (деревьев 1, веток s/* 0, обе заявленные в состоянии пары содержат задачу класса B и по
  ADR-018 не параллелятся вовсе).
  Гейт заострён до проверяемой формулировки; первым шагом по гейту идёт скрипт проверки
  пересечения, а не заведение дерева. Разбор — reference/ADJ_ADR-025_parallel_2026-08-07.md
- ADR-026: Задача существует ⇒ строка в роудмапе с проверяемой приёмкой · accepted
- ADR-027: Определение берётся из репозитория, не из текста запуска; правка по якорям — со
  сплошным поиском понятия и решением по каждому совпадению · accepted
  (прецедент собственный: восемь неверных утверждений текста запуска этой сессии, плюс остаток
  решения о дампах, снятый через одиннадцать дней отдельным решением)

## NEW_CONVENTIONS  (→ предложение правки 05; proposed — не применять без апрува)
- Патчи П-2 … П-7 к 05_CONVENTIONS: роли и мандат по классу · ритуал старта со скриптом статуса
  и проверкой подмены · правило языка ответов владельцу (в конвенциях его СЕЙЧАС НЕТ) ·
  четыре правила наблюдения · два режима чтения репозитория · новый формат session-блока
  со стенд-апом и маркерами времени прихода.
  Все — следствия решений ADR-018, ADR-019, ADR-021, ADR-022, ADR-023 и применяются вместе с ними.
  Местная находка сохранена дословно: INFRA_PATCH обязателен всегда, пустой = явное
  «нет изменений». У метода-донора такого нет.

=== END SESSION ===
```
