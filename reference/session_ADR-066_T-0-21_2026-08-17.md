=== SESSION LOG · 2026-08-17 · ADR-066 (продолжение сессии T-2-0a) ===

## SESSION_LOG
- Задача: закрытие двух развилок `ADR-065` — размещение и обработка исходного ТЗ; упаковка интервью по `Q-29`
- Роль и модель сессии: архитектор · Opus 5 (`claude-opus-5`)
- Коммит на входе: `5e8f15dd` (собственный коммит той же сессии), дерево чистое
- Сделано:
  - заведён локальный каталог `spec/` с описью, по образцу `audit/`; `.gitignore` дополнен;
  - заведена строка карты `T-0-21` — опись ТЗ, разнесение каждого требования в один из четырёх
    исходов, критический разбор со ступенями усиления; приёмка называет, что печатается;
  - в `04 §0` записано **второе названное исключение правила допуска**: работа, предметом которой
    является полнота самого контракта, пункта контракта иметь не может;
  - упакован сценарий интервью с Эльханом: три обязательных блока, один попутный, бланк ответов,
    инструкция проводящему;
  - принят `ADR-066`, строка в `06_INDEX`; назначены гейты `Q-28` и `Q-29` в обоих файлах реестра.
- Команды/логи ключевые: правки документов; ни одного обращения к внешним ресурсам.
- Отклонения от плана: одно, названо в решении п.1. Владелец просил «добавлю его как есть в репо»;
  исполнено буквой в части рабочего дерева и **отклонено в части коммита** — состав ТЗ не измерен
  ни разу, публичный репозиторий необратим. Расхождение с формулировкой владельца названо прямо,
  а не сглажено.

## STATE_PATCH  (→ 07_STATE.md)
- Задача `T-0-21`: (не существовала) → **todo**, ждёт файла ТЗ в `spec/`
- Стенд-ап: заменён целиком, пять строк
- Открытые вопросы: `Q-28` — гейт назначен `T-0-21`; `Q-29` — гейт назначен интервью и разбором
  ответов. Новых строк не заводится
- Блокеры: нет. `T-0-21` вне нитки; `Q-29` держит два сообщения из тринадцати
- В архив: нет
- updated_at: 2026-08-17
- обновил: архитектор (сессия: `T-2-0a` / `ADR-066`)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений: сессия не касалась ни одного ресурса контура клиента и ни одного облачного ресурса.

## NEW_DECISIONS  (→ 06_DECISIONS_LOG.md)
- ADR-066: ТЗ в локальный каталог `spec/`; заведена `T-0-21`; второе исключение правила допуска; интервью по `Q-29` упаковано
  Контекст: владелец кладёт ТЗ и просит не канонизировать его, а декомпозировать в бэклог с
  критическим разбором; `Q-29` закрывается разговором с Эльханом, нужен упакованный сценарий.
  Решение: каталог `spec/` локальный с описью; `T-0-21` с четырьмя исходами разнесения и ступенями
  усиления; ТЗ каноном не объявляется — замер и решение владельца старше; правило допуска получает
  второе названное исключение; сценарий интервью написан, ответы кладутся в `spec/`.
  Статус: accepted

## NEW_CONVENTIONS
- нет.

=== END SESSION ===
