# FILE: reference/session_ADR-028_2026-08-08.md

=== SESSION LOG · 2026-08-08 · ADR-028 ===

## SESSION_LOG
- Задача: разбор гейтов задачи `M-6` и снятие блокировки; роль — архитектор, класс A.
- Коммит на входе: `32624be14bb8c84d00816b175db6a8104084a16c`; дерево на входе чистое.
- Сверка определения (`ADR-027`): обе цитаты текста запуска — строка «Следующий шаг» и строка `Q-12` —
  совпали с `07_STATE.md` дословно. Расхождений нет.
- Сделано:
  - Замер: открытых задач класса A шесть; поле «Файлы на запись» несут только брифы трёх ЗАКРЫТЫХ
    задач; все четыре коммита закрытых задач фазы M правили `07_STATE.md`, ни один бриф этого файла
    не объявлял.
  - Артефакт разбора `reference/ADJ_M-6_gates_2026-08-08.md`: три гейта, по каждому 3 варианта с
    плюсами, минусами, последствиями и рекомендацией.
  - Принято `ADR-028`; применены все последствия «[сейчас]» одним коммитом.
  - Починен третий дефект хука (фантомный идентификатор из глоба трека), самотест расширен.
  - Снято ложное утверждение состояния о неопубликованности коммитов фазы M.
- Команды/логи ключевые: `bash tools/hooks/selftest.sh` → «кейсов проверено: 35 · провалено 0»;
  `bash tools/hooks/pre-commit` на индексе → «ХУК: пройдено, нарушений нет» на каждом из трёх
  коммитов; `git fetch origin` → удалённая ветка на `32624be`, позади 0, впереди 3.
- Отклонения от плана: три. (1) Коммит решения был завален собственным хуком — дефект починен в
  причине, не обойдён текстом. (2) Проверка на секреты дважды завалила правку хука на его же
  исходнике (имя переменной, затем цитата шаблона в комментарии) — оба раза уступлено проверке.
  (3) `git commit --amend` несовместим с проверкой 4 — правка состояния положена отдельным коммитом.

## STATE_PATCH  (→ 07_STATE.md, применён)
- Задача `M-6`: расщеплена на `M-6a` (todo, класс A, гейта нет) и `M-6b` (todo, класс A, гейт `Q-12`).
- Стенд-ап заменён целиком, пять строк; знаменатель счётчика фазы M — 7.
- Подробности для модели: три новые строки — расщепление задачи; общий сериализуемый ресурс вне
  наборов «Файлы на запись» с замером-основанием; третий дефект хука с логом; снятое утверждение о
  публикации.
- Открытые вопросы: `Q-12` сужена до буфера и прохода сборки, скрипт из строки вышел. Новых нет.
- Блокеры: без изменений (пароль БД, `M-5`).
- В архив: нет.
- updated_at: 2026-08-08 · обновил: архитектор (сессия: ADR-028)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений: облачных ресурсов, секретов и подключений сессия не касалась.

## NEW_DECISIONS  (→ 06_DECISIONS_LOG.md, применено)
- ADR-028: Скрипт проверки пересечения выходит из-под гейта; состояние и журнал — общий
  сериализуемый ресурс вне наборов «Файлы на запись».
  Контекст: два круга и одна ложная зелень, все три предъявлены замером.
  Решение: расщепление задачи; исключение общего ресурса из наборов с обязательной печатью его
  отдельной строкой; буфер и проход сборки остаются отложенными.
  Последствия: `04_ROADMAP`, `05_CONVENTIONS`, `07_STATE`, `07_GAPS`, `08_TASK_BRIEF_TEMPLATE`,
  `09_RUNBOOK`, `briefs/_GENERATOR.md`, `.claude/agents/generator.md` — правлены сейчас;
  `tools/intersect_check.sh` — задачей `M-6a`; буфер и роль сборки — задачей `M-6b`.
  Статус: accepted.

## NEW_CONVENTIONS  (→ 05_CONVENTIONS.md, применено решением ADR-028)
- §I, раздел про рабочее дерево: скрипт проверки пересечения выведен из отложенной части; добавлен
  абзац об общем сериализуемом ресурсе и о том, почему молчание о нём есть тихий отказ.

=== END SESSION ===
