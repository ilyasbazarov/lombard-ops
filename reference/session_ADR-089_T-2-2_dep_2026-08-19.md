# FILE: reference/session_ADR-089_T-2-2_dep_2026-08-19.md

```
=== SESSION LOG · 2026-08-19 · ADR-089 · зависимость T-2-2 и место формулы статуса ===

## SESSION_LOG
- Задача: без строки карты — снятие CONTEXT GAP сборки брифа T-2-2
- Роль и модель сессии: архитектор · opus
- Коммит на входе: e9cf06f (дерево чистое)
- Сделано:
  · Остановка генератора признана правильной; зависимость T-2-2 ← T-1-3 проверена впервые с
    момента записи и оказалась НЕВЕРНОЙ. Установлено чтением репозитория:
    (1) compute_status(due_date, today) в functions/cf_daily/status.py — чистая функция двух
        величин, без БД и без состояния;
    (2) canonical view (T-1-2a) отдаёт плановую дату закрытия и смещение от неё — оба входа;
    (3) хранимой колонки статуса нет ни в одной таблице sql/ddl/ (loans_raw несёт status_raw,
        сырое состояние PawnShop, не светофор) — значит T-1-3 статус НЕ приземляет;
    (4) реестру нужны формула и её входы, а не события конвейера; события читает карточка T-2-3.
  · ADR-089 принят: зависимости T-2-2 — T-2-1, T-1-2b; статус 03 §1 вычисляется ОДИН раз в
    canonical view, потребители читают колонку; эквивалентность SQL и Python входит в приёмку
    T-1-2a таблицей смещений −10…+70; за Python остаётся адресат алерта, не статус.
  · Сплошная проверка зависимостей нашла ещё две висячие ссылки на снятую ADR-084 строку T-1-2
    (T-1-3, T-1-6) — обе переведены на T-1-2b.
  · Счётчик 6 tools/state_hot_check.sh: висячая зависимость. Первая версия дала девять ложных
    срабатываний двух видов — закрытая задача, чья строка уехала в архив, и идентификатор внутри
    пояснения в скобках; оба источника сняты и названы в комментарии скрипта.
- Команды/логи ключевые: bash tools/state_hot_check.sh --selftest (14/0);
  bash tools/state_hot_check.sh (0 · 0 · 0 · без мандата 0 · висячих 0)
- Отклонения от плана: нет

## STATE_PATCH  (→ 07_STATE.md)
- Задача: строк карты не заводилось
- Стенд-ап: ЗАМЕНЁН целиком, пять строк
- Подробности для модели: новый пункт [якорь: T-1-2a, T-1-3, T-2-2] — формула статуса в одном месте
- Снято с горячего: нет
- Открытые вопросы: нет
- Блокеры: нет
- В архив: нет
- updated_at: 2026-08-19
- обновил: архитектор (сессия: ADR-089)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md)
- ADR-089: Реестр T-2-2 зависит от наполненного canonical view, а не от конвейера T-1-3; статус
  03 §1 вычисляется один раз — в canonical view
  Статус: accepted

## NEW_CONVENTIONS  (→ 05_CONVENTIONS.md)
- нет
=== END SESSION ===
```
