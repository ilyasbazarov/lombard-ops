# T-1-0 · Сквозная нитка (минимальная) — 2026-08-18

Бриф: `briefs/T-1-0.md`. Класс задачи A (код) · B (деплой). Эта сессия закрывает шаги 1-5
(класс A: код и локальные тесты) и готовит шаги 6-7 (класс B: деплой, подтверждение владельца
не запрошено в этой сессии — только подготовка). **Шаги 8-9 (наблюдение двух автоматических
прогонов) физически невозможны без прошедшего деплоя и НЕ исполнялись.**

## Правило выбора одного договора (шаг 4, пункт «в» брифа)

Первый по возрастанию `contract_id` (числовое сравнение) с непустым `due_date` среди строк,
загруженных из самого свежего блоба `agent/<дата>/CONTRACTS.ndjson`. Реализация —
`functions/cf_daily/main.py::select_one_contract`.

## Частичность маппинга `CONTRACTS` → `loans_raw` (сознательная, не дефект)

Заполнены ровно колонки, выводимые из ОДНОЙ таблицы `CONTRACTS` (`02_DATA_CONTRACTS.md` §3):

| Колонка `loans_raw` | Источник | Заполнена? |
|---|---|---|
| `contract_id` | `CONTRACTS.CONTRACT_ID` | да |
| `issue_date` | `CONTRACTS.CONTRACT_DATE` | да |
| `due_date` | `CONTRACTS.PLAN_CLOSE_DATE` | да |
| `loan_amount` | `CONTRACTS.DEPOSIT_SUM` ÷ 100 | да |
| `vehicle_id` | `SUBJECTS`/`CUSTOM_FIELDS_VALUES` (`T-1-2`) | **NULL** |
| `client_name` | ~~`SUBJECTS`~~ — **источник указан НЕВЕРНО, поправка `ADR-083` п.10-а** | **NULL** |
| `vehicle_make` / `vehicle_model` / `vehicle_year` | `SUBJECTS`/`CUSTOM_FIELDS_VALUES` (`T-1-2`) | **NULL** |
| `status_raw` | `CONTRACT_STATES` (`T-1-2`) | **NULL** |
| `renewed` / `renewed_date` | `OPERATIONS`/`CONTRACTS.ID_PREV_CONTRACT` (`T-1-2`) | **NULL** |

Полная канонизация — предмет `T-1-2`, не этой задачи.

**ПОПРАВКА 2026-08-19 (`ADR-083` п.10-а). Замер этой задачи не переписывается (`ADR-024`) — поправляется
одно утверждение о СХЕМЕ, замером не подтверждённое.** Строка `client_name` называла источником
`SUBJECTS`. Это неверно: `SUBJECTS` есть предмет залога, а не клиент — `TABLES_TABLE.ID=9` →
`SUBJECTS` («Loan items table»), измерено `T-0-4a` (`11_INFRA_FACTS`). Действующий вердикт: у
`client_name` источника в девяти разрешённых таблицах НЕТ вовсе; `CONTRACTS.CLIENT_ID` существует,
таблица клиентов вне белого списка и вне грантов, имя клиента есть ПДн. Колонка висит на `Q-30`,
гейт `T-1-2`. Остальные строки таблицы поправку не получают: `status_raw` (`CONTRACT_STATES`) и
`vehicle_id`/`vehicle_make`/`vehicle_model`/`vehicle_year`
(`CUSTOM_FIELDS_VALUES`/`DIR_CUSTOM_FIELDS`) имеют источники внутри белого списка.

## Решения исполнителя, зафиксированные этой задачей

- **Staircase-интерпретация таблицы порогов `03 §1`** (`functions/cf_daily/status.py`,
  докстринг модуля): между именованными порогами `+2`, `+16`, `+32` статус и адресат держатся
  ступенькой до следующего порога; дни `+39/+46/+62` — точечные события ВНУТРИ 🔴 Реализация,
  где статус не меняется, но адресат сужается до Исы. Это единственное прочтение, дающее
  непрерывную функцию статуса на любое целое смещение, а не только на семь табличных точек.
- **`requirements.txt` без `requests`**: `telegram_send.py` делает ОДИН вызов без сессий и
  ретраев (05 §II «Ограничения» брифа) — стандартной `urllib.request` достаточно, лишняя
  зависимость на холодном старте Cloud Function не оправдана.

## Локальные тесты — вывод команд целиком (не пересказ)

```
$ python3 -m functions.cf_daily.test_bq_loader
[пройдено]  1: parse_gs_path разбирает gs://bucket/blob корректно
[пройдено]  1-БИС: parse_gs_path отбивает не-gs:// путь (контраст с проверкой 1)
[пройдено]  2: map_contract_row — DEPOSIT_SUM делится на 100 строкой, даты копируются
[пройдено]  3: load_contracts_blob — фикстура из 3 строк, счёт и схема БЕЗ autodetect

всего проверок: 4; провалено 0

$ python3 -m functions.cf_daily.test_status
[пройдено]  строка 1: −7 и раньше -> 🟢 Активный, никто не уведомляется
[пройдено]  граница −7 включительно (тот же offset, отдельная проверка формы брифа)
[пройдено]  граница −6 включительно -> уже 🟡 Внимание (переход диапазона)
[пройдено]  строка 2: −6…+1 -> 🟡 Внимание, Бектур (проверка серединой диапазона, +1)
[пройдено]  строка 3: +2 -> 🟠 Просрочен, Бектур + Иса
[пройдено]  строка 4: +16 -> 🟠 Пре-маркетинг, Эльхан + Иса
[пройдено]  строка 5: +32 -> 🔴 Реализация, Эльхан + Иса
[пройдено]  строка 6а: +39 -> 🔴, адресат сужается до Исы (точечная сводка)
[пройдено]  строка 6б: +46 -> 🔴, адресат Иса
[пройдено]  строка 6в: +62 -> 🔴, адресат Иса
[пройдено]  контраст: +40 (день ПОСЛЕ точечного +39) -> 🔴, адресат снова Эльхан + Иса

всего проверок: 11; провалено 0

$ python3 -m functions.cf_daily.test_telegram_send
[пройдено]  1: send_message — успешный ответ Bot API возвращает message_id строкой
[пройдено]  2: send_message — Bot API отвечает ok:false -> TelegramSendError, не тихий проход
[пройдено]  3: модуль telegram_send.py не читает секреты сам — 0 совпадений 'secretmanager'

всего проверок: 3; провалено 0

$ python3 -m functions.cf_daily.test_main
[пройдено]  1: find_latest_contracts_blob выбирает МАКСИМАЛЬНУЮ дату из нескольких каталогов
[пройдено]  2: find_latest_contracts_blob — пустой бакет поднимает исключение, не тихий None
[пройдено]  3: select_one_contract — первый по возрастанию contract_id с непустым due_date
[пройдено]  4: run_pipeline — полный конвейер (а)-(е) на моках всех четырёх клиентов, зелёный путь
[пройдено]  5: run_pipeline — отказ Telegram (шаг д) поднимает PipelineError с именем шага, не тихий успех

всего проверок: 5; провалено 0
```

Зависимости для локального прогона тестов (`google-cloud-storage`, `google-cloud-secret-manager`,
`functions-framework`) установлены этой сессией в пользовательское окружение (`pip3 install --user`)
— это установка на машине исполнителя для тестов, не облачный ресурс и не деплой.

## Секретный поиск перед коммитом (критерий приёмки)

Сплошной поиск слов `telegram-bot-token`-значения, боевых `chat_id` (кроме версии секрета) и
токена бота по всем новым файлам — не найдено. Токен и `chat_id` нигде в коде не хардкодятся:
`main.py` читает их из Secret Manager параметрами в `run_pipeline`.

## НЕЗАКОНЧЕННОЕ состояние

- **Шаг 6 (деплой Cloud Function `cf-daily`) НЕ исполнен** — класс B, ждёт карточки
  подтверждения 1 от владельца. Подготовлен `scripts/T-1-0_deploy.sh part1` (не запущен).
- **Шаг 7 (IAM-биндинг + Cloud Scheduler job) НЕ исполнен** — класс B, ждёт карточки
  подтверждения 2. Подготовлен `scripts/T-1-0_deploy.sh part2` (не запущен).
- **Шаги 8-9 (два автоматических прогона в разные сутки) НЕ наступили** — физически невозможны
  без прошедшего деплоя. Приёмка задачи (`00 §6` пункт 1) остаётся `todo`.
- `gcloud scheduler jobs describe cf-daily-trigger` — не выполнено, ресурс не существует.

Продолжение — та же задача `T-1-0`, тот же артефакт (этот файл дополняется, не переписывается),
после исполнения владельцем карточек подтверждения 1 и 2.
