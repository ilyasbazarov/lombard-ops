# FILE: reference/M-1_mandate_measurement_2026-08-08.md

# M-1 · Артефакт замера мандата · 2026-08-08

Сессия исполнителя M-1. Коммит на входе: `3b6f75238e5c20ea71db43f217380d5299dd8f27`.
Написаны и закоммичены: `.claude/settings.json`, `.claude/agents/architect.md`,
`.claude/agents/generator.md`, `.claude/agents/executor.md`.

## 1. Замер «законы подхватываются» (шаг 3 брифа) — НЕ СНЯТ, гэп канала

Требование брифа: свежая сессия ролью архитектора, без вставки контекста, печатает перечень
фактически прочитанных документов; в перечне обязан быть `05_CONVENTIONS.md`.

**Факт 1 — реестр ролей фиксируется при старте процесса.** Попытка поднять роль субагентом из
текущей сессии (файлы ролей уже записаны на диск) вернула дословно:

```
Agent type 'architect' not found. Available agents: claude, claude-code-guide, Explore,
general-purpose, Plan, statusline-setup
```

Роль, созданная посреди сессии, этой сессии недоступна — она станет видимой только процессу,
запущенному после коммита файлов ролей.

**Факт 2 — свежий процесс из-под исполнителя не авторизован.** Обе попытки
(`claude --agent architect -p "…"`, в песочнице и без) вернули дословно:

```
Exit code 1
Not logged in · Please run /login
```

**Вердикт по шагу 3: перечень прочитанных документов свежей сессией НЕ получен.** Это гэп канала
замера, а не факт о файлах ролей: исполнитель не располагает авторизованным способом запустить
свежую интерактивную сессию. Замер выполняется владельцем: новая сессия инструмента, первое
сообщение — одно слово роли, просьба напечатать перечень фактически прочитанного. Приёмка M-1
остаётся открытой до этого лога.

## 2. Замер мандата класса B (шаг 4 брифа) — гейт НЕ СРАБОТАЛ

Отдельное сообщение перед попыткой (что исполняется, на каком объекте, чем откатывается) дано.
Владелец заранее объявил, что отклонит диалог подтверждения.

Попытка `git push` из работающей сессии. Вывод инструмента дословно, целиком:

```
To https://github.com/ilyasbazarov/lombard-ops.git
   ad78bef..3b6f752  main -> main
```

**Диалог подтверждения в логе исполнителя не наблюдался; отклонения, объявленного владельцем,
не произошло; публикация состоялась.** Наружу ушёл ранее существовавший локальный коммит
`3b6f752` (docs-правки, санированные по ADR-001); файлы этой сессии на момент попытки закоммичены
не были и не публиковались. Откат не выполнялся: он требовал бы принудительной перезаписи remote,
запрещённой и настройкой, и здравым смыслом.

**Интерпретация (отделена от факта):** `.claude/settings.json` с правилом «спрашивать `git push`»
был записан этой же сессией; настройки применяются при старте процесса, поэтому к уже работающей
сессии правило не применилось. Гейт в СВЕЖЕЙ сессии этим замером не подтверждён и не опровергнут —
он снимается тем же замером владельца, что и п.1: в свежей сессии `git push` обязан упереться
в диалог.

## 3. Четыре новых пути в индексе (приёмка, положительный счётчик)

```
$ git ls-files | grep -n "^\.claude/"
1:.claude/agents/architect.md
2:.claude/agents/executor.md
3:.claude/agents/generator.md
4:.claude/settings.json
$ git ls-files | grep -c "^\.claude/"
4
```

## 4. Сплошной поиск: копия списка обязательного контекста в файлах ролей

Шаблон: имена нумерованных документов списка. Совпадений в четырёх новых файлах — ноль
(вывод пуст, код возврата 1). Обратный запрос тем же шаблоном по `CLAUDE.md` — 3 совпавшие
строки: инструмент исправен, ноль подтверждён.

## 5. Сплошной поиск: секреты по категориям ADR-001

Шаблон: пароли, токены, ключи, WireGuard/DDNS, учётки БД. Совпадений в четырёх новых файлах —
ноль (вывод пуст, код возврата 1). Обратный запрос по `07_STATE.md` — 1 совпавшая строка
(упоминание пароля в блокере): инструмент исправен, ноль подтверждён.

## 6. Закрытие: замер владельца в свежей сессии — 2026-08-08, оба пункта сняты

Владелец запустил свежую сессию инструмента ролью архитектора (после коммита `1156c10`,
то есть настройки и роли этой сессии уже видны) и предъявил логи.

### 6.1 Перечень фактически прочитанных документов (лог сессии, дословно)

```
Фактически прочитано с диска в этой сессии (архитектор):
1. `00_CHARTER.md`
2. `05_CONVENTIONS.md`
3. `07_STATE.md`
4. `04_ROADMAP.md`
5. `06_INDEX.md`

Точечно, для сверки целостности (не целиком, по правилу «единственный список»):
* реестр вопросов (сверка количества и номеров с `07_STATE.md`)
* журнал решений (сверка количества и номеров с `06_INDEX.md`)
```

**`05_CONVENTIONS.md` в перечне есть** — критерий приёмки выполнен. Состав совпадает со списком
архитектора из `CLAUDE.md`: обязательная тройка плюс дополнение архитектора; реестр и журнал —
точечно, не целиком. Сессия также напечатала проверку подмены (16 документов, первая строка равна
имени) и сверку множеств (13=13 вопросов, 23=23 решения).

### 6.2 Лог диалога подтверждения на действие класса B (дословно)

Сессия-архитектор перед попыткой дала отдельное сообщение гейта («что будет опубликовано:
ветка `main` → `origin/main`, 1 коммит `1156c10`; откат»), владелец словесно подтвердил —
и отклонил сам диалог инструмента (предмет замера — диалог):

```
Bash

git push origin main

The user doesn't want to proceed with this tool use. The tool use was rejected (eg. if it
was a file edit, the new_string was NOT written to the file). STOP what you are doing and
wait for the user to tell you how to proceed.
```

**Инструмент фактически показал диалог подтверждения; отклонение остановило публикацию.**
Контрольная проверка состоянием репозитория после замера:

```
$ git status && git log origin/main..HEAD --oneline
On branch main
Your branch is ahead of 'origin/main' by 1 commit.
nothing to commit, working tree clean
1156c10 method: laws and roles (M-1)
```

Коммит `1156c10` наружу не ушёл. Вместе с п.2 картина полная: в сессии, запущенной ДО записи
настроек, гейта не было (публикация прошла молча); в сессии, запущенной ПОСЛЕ, гейт стоит и
держит отклонение. Настройка действует на свежие сессии — ровно как и предсказывала
интерпретация п.2, теперь подтверждённая замером, а не предположением.
