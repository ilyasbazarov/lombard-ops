# T-2-4 · Регрессия импортёра после добавления `price_source` — 2026-08-20

**Объект замера определён:** `tools/catalog_import.py` — существовал до этой сессии
(`reference/T-2-4_catalog_import_mechanism_2026-08-13.md`, четыре прогона 2026-08-13).

**Зачем:** шаг 3 брифа `T-2-4` — каждая строка сида, произведённая скриптом из CSV, несёт
`"price_source": "client"`. Прогон тех же четырёх сценариев, что в артефакте 2026-08-13, чтобы
подтвердить: добавленное поле не ломает ни один.

## Прогон 1 — файла нет: CONTEXT GAP, код возврата 2 (совпадает с эталоном)

```
$ python3 tools/catalog_import.py --input /tmp/t24_regress/nonexistent.csv --output /tmp/t24_regress/seed1.json
CONTEXT GAP: файл справочника ликвидности отсутствует и угадыванию не подлежит.
  ожидался путь : /tmp/t24_regress/nonexistent.csv
  ...
код возврата: 2
```

## Прогон 2 — три строки (зелёный/жёлтый/стоп-класс): принято 3, отклонено 0, поле `price_source: "client"` на КАЖДОЙ строке

```
$ python3 tools/catalog_import.py --input /tmp/t24_regress/cat_ok.csv --output /tmp/t24_regress/cat_seed.json
шапка совпала: Марка,Модель,Ликвидность,LTV макс,Скупочная база,Комментарий
строк принято : 3
строк отклонено: 0
распределение по классам: green=1, red=1, yellow=1
сид записан: /tmp/t24_regress/cat_seed.json (3 строк)
код возврата: 0
```

Фрагмент сида (обе крайности):
```json
{ "make": "Toyota", "model": "Camry 70", "liquidity_class": "green",
  "ltv_max": 0.75, "buyout_price": 18000.0, "max_loan_by_catalog": 13500.0,
  "comment": "Уходит за 3 дня", "price_source": "client", "source_row": 2 }
{ "make": "Range Rover", "model": "Sport", "liquidity_class": "red",
  "ltv_max": null, "buyout_price": null, "max_loan_by_catalog": null,
  "comment": "Не берём", "price_source": "client", "source_row": 4 }
```

## Прогон 3 — битая шапка (`LTV max` вместо `LTV макс`): отказ с печатью обеих сторон, код возврата 2

```
$ python3 tools/catalog_import.py --input /tmp/t24_regress/cat_bad_header.csv
ОТКАЗ: шапка не совпала. Обе стороны печатаются целиком:
  ожидалось: ['Марка', 'Модель', 'Ликвидность', 'LTV макс', 'Скупочная база', 'Комментарий']
  в файле  : ['Марка', 'Модель', 'Ликвидность', 'LTV max', 'Скупочная база', 'Комментарий']
код возврата: 2
```

## Прогон 4 — битые строки называются номером и причиной: принято 1, отклонено 3

```
$ python3 tools/catalog_import.py --input /tmp/t24_regress/cat_bad_rows.csv --output /tmp/t24_regress/seed4.json
шапка совпала: Марка,Модель,Ликвидность,LTV макс,Скупочная база,Комментарий
строк принято : 1
строк отклонено: 3
  строка 3 — класс ликвидности не распознан: «фиолетовый»
  строка 4 — класс green требует и LTV, и скупочную базу
  строка 5 — скупочная база — не число: «дорого»
распределение по классам: green=1
код возврата: 0
```

## Вывод

Все четыре прогона совпадают по коду возврата и форме вывода с эталоном 2026-08-13. Единственное
отличие — новое поле `price_source: "client"` в каждой принятой строке сида, ничего не отклонило и
ни одного отказа не вызвало.
