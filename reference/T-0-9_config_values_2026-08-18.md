# T-0-9 · Сверка значений `connector/config.json` с `03_BUSINESS_SPEC.md` — 2026-08-18

Коммит на входе: `67526b262cda3e4ad7b86139b538702b603432ac`.

## Шаг 1 — источник (текущая редакция, `ADR-070`, не `ADR-057` п.4)

`03_BUSINESS_SPEC.md §1`, таблица порогов (строки 19–27):

```
| Смещение от плановой даты | Прежний день цикла (при сроке 29 дней) | Статус | Кто уведомляется | Действие |
|---|---|---|---|---|
| −7 и раньше | 0–22 | 🟢 Активный | — | тишина |
| −6 … +1 | 23–30 | 🟡 Внимание | Бектур | «Позвони клиенту, договор истекает через X дней» |
| +2 | 31 | 🟠 Просрочен | Бектур + Иса | информационно |
| +16 | 45 | 🟠 Пре-маркетинг | Эльхан + Иса | Эльхану чеклист; Исе информационно |
| +32 | 61 | 🔴 Реализация | Эльхан + Иса | протокол реализации + три цены |
| +39 / +46 / +62 | 61+7 / +14 / +30 | 🔴 | Иса | нет предложений / рекомендация −5% / сводка — информационно |
| любое | любой | 🔴 особый | Иса | ⚠️ предложение НИЖЕ floor — единственный случай кнопок |
```

`03_BUSINESS_SPEC.md`, строка 7: «**Порог свежести данных — 26 часов** (назначен `ADR-057`:
суточное расписание плюс запас на прогон и перезапуск; первая ложная тревога есть основание
пересмотра).»

`03_BUSINESS_SPEC.md §4`, строка 181: «Дефолты: floor 🟢 85%, 🟡 90%; износ −1% за 10 дней с порога
реализации (`§1`, смещение +32); realization_price не ниже floor.»

`ADR-057` п.4 (устаревшая терминология, названа явно, не подставлена): «алерты 23/31/45/61» —
абсолютные дни цикла старой модели; `ADR-070` переписал `03 §1` в систему отсчёта «смещение от
плановой даты закрытия». В дело идут значения ТЕКУЩЕЙ редакции `03` (смещения −6/+2/+16/+32), не
литерал `23/31/45/61`.

## Шаг 3 — построчное сравнение (файл vs спецификация)

```json
{
  "status_offsets_days": {
    "attention": -6,
    "overdue": 2,
    "premarketing": 16,
    "realization": 32
  },
  "floor_pct": {
    "green": 0.85,
    "yellow": 0.90
  },
  "depreciation_per_10_days": 0.01,
  "staleness_threshold_hours": 26
}
```

| Ключ файла | Значение файла | Строка `03` | Совпадение |
|---|---|---|---|
| `status_offsets_days.attention` | −6 | §1, строка «−6 … +1 · 🟡 Внимание» | да |
| `status_offsets_days.overdue` | +2 | §1, строка «+2 · 🟠 Просрочен» | да |
| `status_offsets_days.premarketing` | +16 | §1, строка «+16 · 🟠 Пре-маркетинг» | да |
| `status_offsets_days.realization` | +32 | §1, строка «+32 · 🔴 Реализация» | да |
| `floor_pct.green` | 0.85 | §4, «floor 🟢 85%» | да |
| `floor_pct.yellow` | 0.90 | §4, «🟡 90%» | да |
| `depreciation_per_10_days` | 0.01 | §4, «износ −1% за 10 дней с порога реализации» | да |
| `staleness_threshold_hours` | 26 | строка 7, «Порог свежести данных — 26 часов» | да |

## Шаг 4 — проверка отсутствия `chat_id`

```
$ grep -c chat_id connector/config.json
0
$ grep -c floor_pct connector/config.json     # обратный контроль исправности grep
1
$ python3 -c "import json; print(list(json.load(open('connector/config.json'))))"
['status_offsets_days', 'floor_pct', 'depreciation_per_10_days', 'staleness_threshold_hours']
```

`chat_id` в файле отсутствует — предъявлено печатью нулевого совпадения при исправном grep (файл в
той же команде даёт `floor_pct` → 1 совпадение).

## Ограничение шага 8

Пункт приёмки роудмапа «правка порога в файле меняет поведение прогона без деплоя — предъявлено
одним прогоном до и после» этой сессией НЕ исполним: `functions/cf_daily/main.py` на дату брифа
читает пороги статуса из констант `status.py`, а не из `connector/config.json` — рантайм файл не
читает, прогона, меняющего поведение правкой файла, физически не существует (`ADR-079`). Чтение
файла рантаймом — предмет `T-1-3`/`T-1-4`, вне scope `T-0-9`.

## Класс B (не исполнен в этой сессии)

Заливка `connector/config.json` в `gs://${PROJECT_ID}-config/config.json` — облачный ресурс, класс
B, ждёт подтверждения владельца отдельной карточкой (шаг 6 брифа `T-0-9`).

## Шаг 6 (класс B) — заливка в GCS, исполнена

**Подтверждено владельцем.** Объект новый (первая заливка), откат — `gsutil rm`.

```
$ gsutil cp connector/config.json gs://project-c451b48a-07ae-4de4-961-config/config.json
Operation completed over 1 objects/251.0 B.

$ gsutil cat gs://project-c451b48a-07ae-4de4-961-config/config.json
{... совпадает с connector/config.json побайтово ...}
```

`T-0-9` закрыта целиком: классы A и B оба исполнены и приняты.
