=== SESSION LOG · 2026-08-17 · T-0-8 ===

## SESSION_LOG
- Задача: `T-0-8` — канал прод-подключения: агент на сервере + единственная точка доступа
- Роль и модель сессии: исполнитель · sonnet (`ADR-030`, поле `model` в `.claude/agents/executor.md`)
- Коммит на входе: `c1ee805`
- Сделано:
  - Предъявлен основной прогон агента (шаг 7, вторая половина) — лог
    `run_daily_20260817_181717.err.log`, дословно, от владельца: 9 запросов `SELECT`, режим
    транзакции подтверждён `MON$READ_ONLY=1` на каждый запрос, отпечаток схемы совпал для всех
    9 таблиц разом.
  - Выполнен второй вход шага 7 (класс B, чтение) — `gcloud storage ls --recursive` по префиксу
    `agent/2026-08-17/`: 9 объектов, имена совпадают с таблицами лога.
  - Написан и предъявлен артефакт `reference/T-0-8_step7_agent_run_2026-08-17.md` — сверка числа
    строк лог↔бакет, разбор каждого пункта приёмки шага 7.
  - Закрыт шаг 8 (фиксация знания): патч `10_GCP_INFRA_PLAYBOOK.md` (раздел «Сетевой канал…») и
    `INFRA_PATCH` в `11_INFRA_FACTS.md` (два новых факта: точечное ограничение `icacls` только на
    `private_key.pem`/`credentials.json`, `OAuthError` не подкласс `RefreshError`).
  - Заведена строка реестра `Q-27` (`07_GAPS.md` + `07_STATE.md` индекс) по третьему факту раздела 6
    отрицательных проб — наследование прав на `signed_jwt.txt` не разорвано; мера не исполнена, вне
    радиуса шага 7.
  - `07_STATE.md`: стенд-ап заменён целиком, мандатная таблица (`T-0-8` → done), таблица очереди
    нитки (`T-0-8` снята, `T-0-4a`/`T-0-18`/`T-1-0` сдвинуты на 1–3), счётчик пересчитан.
- Команды/логи ключевые: `gcloud storage ls --recursive gs://project-c451b48a-07ae-4de4-961-cfsource/agent/2026-08-17/` (9 объектов, класс B, карточка подтверждена владельцем перед исполнением).
- Отклонения от плана: нет — передача `reference/T-0-8_step7_handoff_executor_2026-08-17.md`
  исполнена по всем пунктам §3–§5, вне радиуса (§6) ничего не тронуто.

## STATE_PATCH  (→ 07_STATE.md)
- Задача `T-0-8`: **todo (шаг 7 остался)** → **done** (все восемь шагов)
- Стенд-ап (ЗАМЕНЯЕТ блок целиком, ровно пять строк) — применено файлом напрямую этой сессией:
  - Прошлый шаг: `T-0-8` закрыта целиком — шаг 7 и шаг 8, артефакты
    `reference/T-0-8_step7_agent_run_2026-08-17.md` и `..._negative_probes_2026-08-17.md`.
  - Где мы: нитка до первого живого сообщения — `T-0-4a`, `T-0-18`, `T-1-0`, по одной.
  - Следующий шаг: `T-0-4a` — первая по очереди `04 §7`, брифа нет, заводится генератором.
  - Развилки на владельце: мера `signed_jwt.txt` (`Q-27`, класс B); переезд чтения БД в класс A —
    факты предъявлены, решение не запрошено этой сессией; плюс прежнее по каталогу `audit/`.
  - Счётчик: открытых строк карты 28 (было 29); в фазе 0 закрыто 9 (было 8); открытых вопросов 10
    (было 9, добавлен `Q-27`); до даты поставки 14 дней.
- Подробности для модели: **третий факт условия переезда чтения БД в класс A (`ADR-033`, форма
  `ADR-055`) ПРЕДЪЯВЛЕН ОБЕИМИ ЛОГАМИ** — тест точки доступа (отказ на запрещённом операторе,
  пропуск разрешённого; `reference/T-0-8_step7_negative_probes_2026-08-17.md`) и реальное чтение
  через точку доступа со счётом строк (`reference/T-0-8_step7_agent_run_2026-08-17.md`, раздел 4).
  **Ответ: да, третий факт предъявлен обоими логами.** Сам переезд этой сессией НЕ производится —
  он требует отдельной строки решения, и брифом не назначен этой задаче.
- Открытые вопросы: заведена `Q-27` (OPEN, гейт — мера класса B не назначена) в `07_STATE.md` и
  `07_GAPS.md` одним коммитом.
- Блокеры: нет.
- В архив: нет — `T-0-8` снята из таблицы очереди нитки, но сама мандатная строка и артефакты
  остаются в `07_STATE`/`reference/` до отдельного переноса в `07_ARCHIVE.md`; эта сессия перенос
  не делает.
- updated_at: 2026-08-17
- обновил: исполнитель (сессия: `T-0-8`, закрытие шагов 7–8)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- Строка «Способ запуска агента»: дополнена фактом исполненного основного прогона шага 7
  (9 таблиц, первая попытка каждая, ссылка на `reference/T-0-8_step7_agent_run_2026-08-17.md`).
- Новая строка «Права на файлы учётных данных агента»: `icacls` ограничивает точечно только
  `private_key.pem` и `credentials.json`; каталог `keys` наследование не разрывает; `signed_jwt.txt`
  читаем `BUILTIN\Пользователи` в пределах TTL. Мера не исполнена, `Q-27`.
- Новая строка «Обработка отказа платформы на просроченном/несвежем JWT»: `OAuthError` не подкласс
  `RefreshError` в установленной версии `google-auth` — обработчик обязан ловить оба класса.

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md)
- нет.

## NEW_CONVENTIONS
- нет.
=== END SESSION ===
