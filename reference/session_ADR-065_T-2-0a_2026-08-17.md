=== SESSION LOG · 2026-08-17 · T-2-0a ===

## SESSION_LOG
- Задача: `T-2-0a` — контракт взаимодействия (экраны, сообщения, роли)
- Роль и модель сессии: архитектор · Opus 5 (`claude-opus-5`)
- Коммит на входе: `00e058fc1ed98344cc5887f921e213d873dce0ed`, дерево чистое
- Сделано:
  - прочитан обязательный контекст с диска: `00`, `05`, `07_STATE`, плюс архитекторское дополнение
    `04`, `06_INDEX`, `01`, `02`, `03` и точечно `07_GAPS`;
  - **диагноз запроса владельца предъявлен, а не принят на веру:** разрез «пункт контракта →
    задачи», критический путь и приёмки по каждой строке уже существуют — переписывать бэклог
    значило бы завести вторую копию карты; названы три настоящие дыры вместо одной заявленной;
  - создан `12_UX_CONTRACT.md` (SEMI-STABLE): карта из шести экранов, состав каждого, матрица
    «роль × экран × действие», каталог из тринадцати сообщений с событиями и ключами
    идемпотентности, переходы состояний осмотра и предложения, три правила показа непроверенных
    величин, обратный разрез на пункты `00 §6`, девять открытых пунктов с гейтами;
  - три сплошных замера прогнаны, артефакт `reference/T-2-0a_ux_contract_sweeps_2026-08-17.md`;
  - принят `ADR-065`, строка в `06_INDEX`;
  - заведены `Q-28` (исходного ТЗ в контуре нет) и `Q-29` (состав чеклиста дня 45 и протокола дня 61);
  - исправлено расхождение карты и состояния по `T-0-8`.
- Команды/логи ключевые:
  - `grep` по девяти типам событий `02 §2` → `IMPORT` дал **0 совпадений**; дефект закрыт явной
    строкой «интерфейсом не порождается вовсе», повторный замер печатает строку 221;
  - `grep -rn "Section7\|техническое задание\|архитектурное ТЗ" --include="*.md"` → **одно
    совпадение на весь репозиторий**, `03_BUSINESS_SPEC.md:4`; исправность инструмента подтверждена
    непустыми выдачами соседних поисков и обратным замером на заведомо отсутствующей строке;
  - `git check-ignore -v audit/APP_B_economics.md` → `.gitignore:14: audit/*`; в репозитории только
    опись — устройство `ADR-063` исполнено, файлы владельцем выложены 2026-08-15.
- Отклонения от плана: два, оба названы.
  (1) **Прямая трассировка «требование ТЗ → пункт контракта» НЕ сделана** — вторая половина варианта,
  выбранного владельцем. Причина не в нехватке времени: документа ТЗ в контуре нет, трассировать не
  от чего. Вместо трассировки заведён `Q-28`; выдумывать состав ТЗ запрещено главным законом.
  (2) **Исправлена чужая недоделка** — строка `T-0-8` в `04_ROADMAP` оставалась `todo` при закрытой
  задаче в состоянии. Правка вне заявленного радиуса, названа поимённо и внесена в решение (п.9),
  потому что карта есть предмет чтения владельцем.

## STATE_PATCH  (→ 07_STATE.md)
- Задача `T-2-0a`: (не существовала) → **done** (заведена и закрыта одним коммитом, `ADR-065`)
- Задача `T-0-8`: `todo` в карте → **done** (состояние объявило закрытие 2026-08-17, карта отстала)
- Стенд-ап: заменён целиком, пять строк
- Подробности для модели: добавлена строка о `12_UX_CONTRACT` — где живёт, кто грузит, граница с
  `T-2-0`, инвариант «у `owner` в вебе действий нет»; обновлены свободные номера
- Открытые вопросы: `Q-28` (точки А в контуре нет, тип «неопределённый объект») и `Q-29` (состав
  чеклиста и протокола, тот же тип) — заведены в `07_STATE` и `07_GAPS` одним коммитом
- Блокеры: новых нет. Ни `Q-28`, ни `Q-29` нитку не блокируют: радиус `Q-28` — обоснование полноты,
  радиус `Q-29` — два сообщения из тринадцати
- В архив: нет (перенос закрытых строк `T-0-8` из `07_STATE` в `07_ARCHIVE` остаётся названной
  задолженностью сессии закрытия `T-0-8`, эта сессия его не делает и не скрывает)
- updated_at: 2026-08-17
- обновил: архитектор (сессия: `T-2-0a`)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений: сессия не касалась ни одного ресурса контура клиента и ни одного облачного ресурса.

## NEW_DECISIONS  (→ 06_DECISIONS_LOG.md)
- ADR-065: Контракт взаимодействия — отдельный документ `12`; `T-2-0a` исполнена; заведены `Q-28` и `Q-29`
  Контекст: между бизнес-правилом `03` и строкой карты `04` нет слоя «что видит человек, что он
  делает, какими словами с ним говорит система и что после этого записывается»; пять задач
  реализации изобрели бы его каждая по-своему.
  Решение: документ `12` заводится, в обязательный контекст не вводится, граница с `T-2-0`
  проведена дословно; `T-2-0a` заведена строкой карты и закрыта; интерфейсные назначения записаны
  решением; трассировка от ТЗ невозможна — `Q-28`; состав чеклиста и протокола не определён — `Q-29`.
  Статус: accepted

## NEW_CONVENTIONS
- нет.

=== END SESSION ===
