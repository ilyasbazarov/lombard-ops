# T-0-18 · Настройка боевого телеграм-контура — 2026-08-18

Бриф: `briefs/T-0-18.md`. Класс задачи B (`ADR-018`, `ADR-056`). Основание класса отправки —
`ADR-056`: боевые чаты без сотрудников клиента, класс отправки A по составу чата; чтение значения
токена остаётся классом B отдельно от этого правила.

## Бот

Создан владельцем через `@BotFather` 2026-08-18: `omega-express-bot`, `@omegaexpress_bot`,
`id 8999667237`. Значение токена в этот файл, в лог сессии и в диалог с Claude не попадало
(красная линия задачи) — записано владельцем напрямую в Secret Manager.

## Боевые чаты

Три группы, заведены владельцем 2026-08-18, в каждой только бот (администратор) и владелец —
сотрудников клиента нет ни в одной (условие `ADR-056` на класс отправки A):

| Роль (`12_UX_CONTRACT.md`) | Название чата | Состав (шаг 6) |
|---|---|---|
| `owner` | Omega Owner | 2 участника: бот + владелец, сотрудников клиента 0 |
| `manager` | Omega Manager | 2 участника: бот + владелец, сотрудников клиента 0 |
| `assessor` | Omega Assessor | 2 участника: бот + владелец, сотрудников клиента 0 |

## Secret Manager — версии (шаг 4)

Метаданные версий, без значений:

```
секрет telegram-bot-token, версия 2, ENABLED, создана 2026-08-18T09:08:49 (версия 1 — плейсхолдер, T-0-7)
секрет chat_id, версия 2, ENABLED, создана 2026-08-18T09:09:07 (версия 1 — плейсхолдер, T-0-7)
секрет firebird-readonly-creds, версия 2, ENABLED, создана 2026-08-13 (версия из T-0-8, шаг 5-В — не эта задача)
```

Схема JSON секрета `chat_id` (ключи — договорённость этой сессии, совпадают с ролями
`12_UX_CONTRACT.md`; значения не публикуются):

```json
{"owner": <chat_id>, "manager": <chat_id>, "assessor": <chat_id>}
```

## Тестовая отправка (шаг 5)

Отправлено в боевой чат `owner` (класс A по составу чата — сотрудников клиента нет, `ADR-056`;
чтение значения токена для этого шага — класс B, подтверждено отдельной карточкой перед
исполнением):

```
message_id: 13
date: 1787044396
chat.id: -5554146763
chat.type: group
```

Ответ API возвращён владельцем в сессию без значения токена (токен идёт в URL запроса, не в тело
ответа). Доставка подтверждена дополнительно фактом появления сообщения в самом клиенте Telegram.

## Состав чатов (шаг 6)

Снято командой `getChatMemberCount` владельцем, без повторного чтения токена в сессию (переменная
терминала переиспользована из шага 5):

- `owner`: `result: 2`
- `manager`: `result: 2`
- `assessor`: `result: 2`

По всем трём — бот + владелец, сотрудников клиента 0.

## Факт R18 (шаг 7)

Владелец подтвердил текстом в сессии 2026-08-18: «передал токен и chat_id Исе лично» — токен бота
и три `chat_id` переданы и зафиксированы у владельца бизнеса (Иса), способ — личная передача.
Снимает bus factor подрядчика (`T-0-21`, требование R18).

## Ссылки

`ADR-056` (боевые чаты сразу, класс отправки по составу чата) — `06_DECISIONS_LOG.md`.
