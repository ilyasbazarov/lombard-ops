=== SESSION LOG · 2026-08-19 · T-1-2a ===

## SESSION_LOG
- Задача: T-1-2a — Канонизация (SQL) + детектор продлений — логика по схеме
- Роль и модель сессии: исполнитель · sonnet (ADR-030, поле `model` из `.claude/agents/executor.md`)
- Коммит на входе: c2cce7e47850a6798c8a2d8d001aa9bd4c90b48b (бриф перегенерирован с требованием
  `ADR-089`; прежняя остановка `CONTEXT GAP` этой же задачи, коммит `6edfe5a8`, снята — требование
  теперь в тексте брифа явно, шаги 7–8 и критерии приёмки его несут)
- Сделано: `sql/canonization.sql` (BigQuery Standard SQL) + офлайн-приёмка на DuckDB
  (`connector/canonize/`) — все 10 шагов брифа и все критерии приёмки закрыты, включая таблицу
  эквивалентности статуса `03 §1` (`ADR-089` п.2) на смещениях `-10…+70`, обе стороны совпали на
  всех 81 строках.
- Команды/логи ключевые:
  - `python3 connector/canonize/test_canonization.py` — 11 проверок, 0 провалено, вывод целиком
    и таблица эквивалентности — `reference/T-1-2a_canonization_logic_2026-08-19.md`.
  - `git status --short` до и после — только объявленные «Файлы на запись» (`sql/canonization.sql`,
    `connector/canonize/`) плюс `/reference`.
- Отклонения от плана: нет — бриф исполнен буквально, включая `ADR-089`.

## STATE_PATCH  (→ 07_STATE.md; поток «данные», правит СБОРКА, не эта сессия — ADR-052 п.4)
- Задача T-1-2a: todo → **done** (2026-08-19, `briefs/T-1-2a.md`, все 10 шагов,
  `reference/T-1-2a_canonization_logic_2026-08-19.md`; 11/11 проверок, включая таблицу
  эквивалентности статуса `-10…+70` SQL vs `compute_status()` — совпадение на каждой строке)
- Стенд-ап (для сборки, к применению целиком):
  - Прошлый шаг: `T-1-2a` закрыта — SQL канонизации (`status_raw`, детектор продлений обе ветви +
    `OR`, дедупликация снапшота, canonical view, статус `03 §1` единственной SQL-реализацией)
    предъявлен офлайн-приёмкой на фикстурах, к BigQuery и к базе клиента не обращался.
  - Где мы: поток «данные» разблокировал `T-1-2b` (наполнение `loans_raw` из слоя сырья + каталог
    полей объекта, класс A/B) и через неё `T-2-2`; потоки «веб» и «баланс» — без изменений этой
    сессией.
  - Следующий шаг: `T-1-2b` (наполнение колонок `loans_raw`, применение view — класс B).
  - Развилки на владельце: без изменений этой сессией (см. предыдущий стенд-ап).
  - Счётчик: без изменений этой сессией — пересчитывает сборка.
- Подробности для модели:
  - **[якорь: T-1-2b]** Пять колонок `loans_raw` (`vehicle_id`, `vehicle_make`, `vehicle_model`,
    `vehicle_year`, `status_raw`) плюс `renewed`/`renewed_date` наполняются `T-1-2b` ИЗ VIEW
    `lombard_ops.v_canonization` (`sql/canonization.sql`, `T-1-2a`, закрыта), не повторным чтением
    базы клиента. `client_name` остаётся `NULL` (`Q-30`), `vehicle_*` в `v_canonization` уже NULL
    явно (наполняются самой `T-1-2b`, не читаются из canonization).
  - **[якорь: T-1-2b]** SQL канонизации использует ДВЕ переиспользуемые функции —
    `lombard_ops.offset_days(today, due_date)` и `lombard_ops.status_for_offset(offset_days)` —
    применять их в BigQuery (шаг `T-1-2b`, класс B) как есть, не переопределять: они и есть
    единственная реализация статуса `03 §1` (`ADR-089` п.2), эквивалентность `functions/cf_daily/
    status.py::compute_status` предъявлена офлайн (`reference/T-1-2a_canonization_logic_2026-08-19.md`).
- Открытые вопросы: нет новых.
- Блокеры: нет новых.
- В архив: строка `T-1-2a` в `07_STATE.md` «Задачи и мандат» переносится в `07_ARCHIVE.md` дословно
  тем же коммитом, которым сборка применит этот патч (`ADR-019`) — сборка решает, снят ли якорь
  `[якорь: T-1-2a]` целиком (два прежних пункта секции «Подробности для модели» несут этот якорь
  вместе с `T-1-0`/другими — сборка проверяет, закрылись ли ВСЕ якоря каждого пункта, не только
  `T-1-2a`, прежде чем убирать пункт в архив).

## INFRA_PATCH  (→ 11_INFRA_FACTS.md) — нет изменений

## NEW_DECISIONS — нет

## NEW_CONVENTIONS — нет
=== END SESSION ===
