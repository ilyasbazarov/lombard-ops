# ADJ · Сплошной поиск понятия «tracer bullet» перед переопределением его позиции (ADR-059)

**Дата:** 2026-08-13 · **Роль:** архитектор · **Задача:** калибровка роудмапа
**Зачем:** `ADR-059` п.4 двигает сквозную нитку с КОНЦА фазы 1 в её НАЧАЛО. `ADR-027` требует:
решение, переопределяющее понятие, применяется не только по названным якорям — обязателен сплошной
поиск с печатью совпавших строк и решением по КАЖДОМУ совпадению.

## Команда и полная выдача (совпадения по «tracer», регистр не важен)

```
grep -rni "tracer" --include="*.md" --include="*.sh" --include="*.py" . | grep -v "^./.git/"

04_ROADMAP.md:6:Порядок фаз: конвейер данных раньше форм (ADR-005). Финиш Фазы 1 = tracer bullet на живых данных (урок референс-проекта: e2e рано, не в конце).
04_ROADMAP.md:66:| T-1-5 | **Tracer bullet**: БД → BQ → статус → живой алерт в Telegram по расписанию | e2e лог зелёный два дня подряд |
09_GLOSSARY.md:31:- **Tracer bullet** — тонкий сквозной срез конвейера e2e на живых данных (финиш Фазы 1).
06_DECISIONS_LOG.md:34:**Решение:** Фаза 1 — конвейер + светофор с tracer bullet в конце фазы; Фаза 2 — приложение и формы; Фаза 3 — Stop-Loss.
06_DECISIONS_LOG.md:1357:   дата закрытия и продления. Основание — собственная строка `04`: «Финиш Фазы 1 = tracer bullet на
06_DECISIONS_LOG.md:1384:2. **Блокер противоречит собственному плану** — строка про tracer bullet стоит в шапке `04`.
06_DECISIONS_LOG.md:1404:фазы 0». Прогнан поиск по `критери`, `БЛОКЕР`, `прод-код`, `tracer`. Решение по каждому совпадению:
06_DECISIONS_LOG.md:1406:`04_ROADMAP.md:6` — **остаётся** (строка про tracer bullet теперь не противоречит блокеру);
06_DECISIONS_LOG.md:1408:`04_ROADMAP.md:58` — **остаётся** (приёмка tracer bullet);
06_DECISIONS_LOG.md:2422:   переопределена: она больше не «tracer bullet», а сквозная проверка на ПОЛНОМ портфеле.
```

Дополнительный поиск по идентификатору:

```
grep -rn "T-1-5" --include="*.md" . | grep -v "^./.git/"

04_ROADMAP.md:66:| T-1-5 | **Tracer bullet**: БД → BQ → статус → живой алерт в Telegram по расписанию | e2e лог зелёный два дня подряд |
06_DECISIONS_LOG.md:2421:   статус, одно сообщение по расписанию) — идёт ПЕРВОЙ строкой фазы 1. Прежняя `T-1-5`
```

**Обратный контроль исправности инструмента** (пустая выдача подтверждается встречным запросом,
`05 §I` «Правила наблюдения»): `grep -rc "конвейер" 04_ROADMAP.md 01_ARCHITECTURE.md` →
`04_ROADMAP.md:2`, `01_ARCHITECTURE.md:1`. Инструмент ищет и находит; нули выше — факт, а не отказ.

## Решение по КАЖДОМУ совпадению

| Совпадение | Решение |
|---|---|
| `04_ROADMAP.md:6` (шапка: «Финиш Фазы 1 = tracer bullet») | **ПРАВИТСЯ.** Формулировка перевёрнута: сквозная нитка — НАЧАЛО фазы 1. Сам урок референс-проекта («e2e рано, не в конце») правкой не затронут — он ровно за перенос, а прежняя строка ему противоречила |
| `04_ROADMAP.md:66` (строка `T-1-5`) | **ПРАВИТСЯ.** Содержание переезжает в новую `T-1-0`; идентификатор `T-1-5` сохраняется и переопределяется в сквозную проверку на ПОЛНОМ портфеле. Идентификатор не переиспользуется вхолостую: обе строки живут в одной фазе и различены предметом (одна нитка против всего портфеля) |
| `09_GLOSSARY.md:31` (определение) | **ПРАВИТСЯ.** «финиш Фазы 1» → «первый шаг Фазы 1»; определение самого понятия не меняется |
| `06_DECISIONS_LOG.md:34` (`ADR-005`) | **НЕ ПРАВИТСЯ** — журнал append-only. Порядок фаз `ADR-005` (конвейер раньше форм) в силе целиком; уточняется ОДНА фраза — позиция нитки внутри фазы 1. Уточнение отмечено в строке `ADR-005` оглавления |
| `06_DECISIONS_LOG.md:1357`, `:1384`, `:1404`, `:1406`, `:1408` (`ADR-044`) | **НЕ ПРАВЯТСЯ** — журнал append-only, это историческая аргументация снятия блокера фазы 0. Она опиралась на строку шапки роудмапа как на факт о ПЛАНЕ; после правки шапки вывод `ADR-044` не меняется: он был «e2e рано», и перенос нитки в начало усиливает его, а не отменяет |
| `06_DECISIONS_LOG.md:2421–2422` (`ADR-059`) | **ОСТАЁТСЯ** — текст самого переопределяющего решения |

**Совпадений всего:** 12 (10 по понятию + 2 по идентификатору, одно общее). Решение принято по
каждому; не рассмотренных нет.
