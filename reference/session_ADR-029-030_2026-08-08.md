# FILE: reference/session_ADR-029-030_2026-08-08.md

=== SESSION LOG · 2026-08-08 · ADR-029 · ADR-030 ===

## SESSION_LOG
- Задача: два решения владельца в одной сессии — заморозка ротации пароля и постоянная витрина
  назначенной модели. Продолжение сессии `ADR-028`, то же рабочее дерево, тот же архитектор.
- Роль и модель сессии: архитектор · дорогая модель (`opus`, поле `model` файла роли).
- Коммит на входе этого отрезка: `b802d78`.
- Сделано:
  - `ADR-029`: `M-5` заморожена решением владельца; строка задачи, строка роудмапа и текст блокера
    перенесены в `07_ARCHIVE.md` дословно; раздел «Блокеры» состояния пуст.
  - `ADR-030`: скрипт старта печатает шестым счётчиком «роль → модель» из файлов ролей и правило
    выбора; шапка брифа получила поле «Кем исполняется»; `SESSION_LOG` получил строку роли и модели.
  - Гейт `Q-13` переписан: прежний («три закрытые задачи фазы M») был выполнен, но негоден —
    закрытые задачи модель не записывали, сравнивать было нечего.
- Команды/логи ключевые: `bash tools/session_status.sh` → счётчик 6 печатает `architect → opus`,
  `executor → sonnet`, `generator → sonnet`; `bash tools/hooks/selftest.sh` → «кейсов проверено: 35 ·
  провалено 0»; хук на каждом коммите → «ХУК: пройдено, нарушений нет».
- Отклонения от плана: нет.

## STATE_PATCH  (→ 07_STATE.md, применён)
- Задача `M-5`: убрана из таблицы (заморожена, `ADR-029`); знаменатель счётчика фазы M — 6.
- Раздел «Блокеры»: содержимое перенесено в архив, раздел несёт явное «нет» со ссылкой на решение.
- Подробности для модели: две новые строки — заморозка с её действующими следствиями; витрина
  модели по ролям.
- Открытые вопросы: `Q-13` — переписан гейт. Новых нет.
- В архив: строка задачи `M-5`, строка роудмапа `M-5`, текст блокера — дословно.
- updated_at: 2026-08-08 · обновил: архитектор (сессия: ADR-028 · ADR-029 · ADR-030)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений: облачных ресурсов и подключений сессия не касалась. Отдельно фиксируется, что
  учётные данные администратора БД остаются прежними по решению владельца — это факт архива, а не
  инфраструктурная правка.

## NEW_DECISIONS  (→ 06_DECISIONS_LOG.md, применено)
- ADR-029: Ротация пароля администратора БД не выполняется; риск принят владельцем, блокер снят.
  Контекст: задача стояла `todo` класса B и ждала подтверждения; владелец заморозил её до конца
  проекта. Решение: строка и блокер уходят в архив, риск принимается сознательно.
  Последствия: `07_STATE`, `07_ARCHIVE`, `04_ROADMAP` — сейчас; проверка на секреты — без правок.
  Отменена в части адресата строка последствий `ADR-022`. Статус: accepted.
- ADR-030: Назначенная модель печатается механически.
  Контекст: ответ в репозитории был, витрины не было; и канал замера для `Q-13` отсутствовал.
  Решение: файл роли — источник истины, скрипт старта — витрина, шапка брифа — доставка,
  session-блок — замер. Последствия: `tools/session_status.sh`, `08_TASK_BRIEF_TEMPLATE`,
  `05_CONVENTIONS` §II и §III, `09_RUNBOOK`, `07_STATE`, `07_GAPS` — сейчас. Статус: accepted.

## NEW_CONVENTIONS  (→ 05_CONVENTIONS.md, применено решением ADR-030)
- §II: абзац «какая модель назначена какой роли — спрашивать не нужно»; источник истины назван,
  расхождение текста с файлом роли разрешено в пользу файла.
- §III: строка «Роль и модель сессии» в `SESSION_LOG`.

=== END SESSION ===
