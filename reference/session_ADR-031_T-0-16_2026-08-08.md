# FILE: reference/session_ADR-031_T-0-16_2026-08-08.md

=== SESSION LOG · 2026-08-08 · ADR-031 · T-0-16 ===

## SESSION_LOG
- Задача: закрыть остатки фазы M по решению владельца и исполнить гигиену репозитория.
- Роль и модель сессии: архитектор · дорогая модель (`opus`, поле `model` файла роли).
- Коммит на входе: `5906d9d`. На входе в дереве лежал незакоммиченный `briefs/M-6a.md`.
- Сверка определения (`ADR-027`): вопрос запуска был про «гейты `M-6a`». Сверка с диском: гейтов у
  `M-6a` нет по `ADR-028`, а сама задача уже закрыта (`tools/intersect_check.sh` и артефакт замера
  на месте, коммиты `f20682a`, `5906d9d`). Расхождение названо владельцу до начала работы.
- Сделано:
  - `briefs/M-6a.md` закоммичен.
  - `Q-1` и `T-0-13` закрыты ответом владельца «второго филиала нет»; обе строки в архиве. Строки
    существовали с первой загрузки репозитория (`7b11404`), сессиями не заводились — проверено
    историей файлов.
  - `ADR-031`: отложенная часть изоляции закрыта ОТКАЗОМ; `Q-12` и `M-6b` в архиве; названо условие
    оживления. Фаза M закрыта полностью.
  - `T-0-16` исполнена: дубль журнала снят (9 строк, блоки сверены побайтово), сплошной поиск по
    трём категориям `ADR-001`, решение по каждому совпадению, раскрытые значения изъяты из рабочего
    дерева. Замер — `reference/T-0-16_sanitation_sweep_2026-08-08.md`.
- Команды/логи ключевые: хук на каждом из пяти коммитов → «ХУК: пройдено, нарушений нет»; обратная
  проверка после изъятия → совпадений значения в рабочем дереве нет; обратная проверка исправности
  поиска → 21 совпадение по контрольной строке.
- Отклонения от плана: одно, названное явно. Бриф ЗАКРЫТОЙ задачи `T-0-2` правлен вопреки общему
  правилу о провенансе закрытых брифов: санитария — красная линия `00 §4`, она старше. Основание
  записано и в решении, и в состоянии.

## STATE_PATCH  (→ 07_STATE.md, применён)
- `Q-1`, `T-0-13`, `Q-12`, `M-6b`, `T-0-16` — убраны из состояния, перенесены в архив дословно.
- Стенд-ап заменён целиком: фаза M закрыта, счётчик 5/5, следующий шаг — доступ к базе.
- Подробности для модели: три новые строки — закрытие вопроса про инсталляцию; отказ от буфера с
  условием оживления; результат гигиены с оговоркой про историю.
- Открытые вопросы: `Q-12` закрыт. Новых нет.
- Блокеры: нет.
- В архив: пять строк, все дословно.
- updated_at: 2026-08-08 · обновил: архитектор (сессия: ADR-031 · T-0-16)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений: инфраструктуры сессия не касалась. Отдельно фиксируется, что изъятие значений из
  рабочего дерева НЕ меняет состояние сервера — учётные данные там прежние (`ADR-029`).

## NEW_DECISIONS  (→ 06_DECISIONS_LOG.md, применено)
- ADR-031: Буфер session-блоков и проход сборки не заводятся; отложенная часть `ADR-025` закрыта
  отказом. Контекст: параллельных сессий не было ни разу, полезная часть связки уже написана
  задачей `M-6a`, вечно-отложенная строка тратит внимание каждого старта. Решение: отказ с
  названным условием оживления. Последствия: `07_STATE`, `07_GAPS`, `07_ARCHIVE`, `04_ROADMAP`,
  `05_CONVENTIONS`, `08_TASK_BRIEF_TEMPLATE`, `09_RUNBOOK` — сейчас; счётчик буфера в скрипте
  старта остаётся без правок. Статус: accepted.

## NEW_CONVENTIONS  (→ 05_CONVENTIONS.md, применено решением ADR-031)
- §I: абзац о буфере переписан с «ждут гейта» на «не заводятся, условие оживления названо».

=== END SESSION ===
