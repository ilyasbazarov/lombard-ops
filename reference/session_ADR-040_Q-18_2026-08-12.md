# Session-блок · 2026-08-12 · разбор несошедшегося оракула `T-0-3b`, `Q-18`, решение `ADR-040`

=== SESSION LOG · 2026-08-12 · T-0-3b (разбор результата) ===

## SESSION_LOG
- Задача: разбор результата `T-0-3b` — оракул не сошёлся; заведение `Q-18` и решение по порядку
  задач. Сама задача исполнена и закоммичена предыдущей сессией (`5eb973d`).
- Роль и модель сессии: архитектор · opus (`ADR-030`; поле `model` в `.claude/agents/architect.md`)
- Коммит на входе: `5eb973d`
- Сделано:
  - обязательный контекст загружен по единственному списку `CLAUDE.md` (роль архитектора), плюс
    точечно `ADR-010 v3`, `ADR-023`, `ADR-033`, `ADR-034`, `ADR-039`, оба артефакта `T-0-3b`,
    `03 §2`/`§4`, `04_ROADMAP`, `06_INDEX`, «Вне scope» брифа `T-0-3b`, список таблиц `T-0-5`;
  - проверка подмены: первая строка каждого нумерованного документа равна его имени — совпало;
  - числа артефакта пересчитаны независимо; гипотеза «сдвинутая точка отсчёта» проверена и
    признана НЕ закрывающей расхождение в одиночку; названа различающая проверка;
  - сплошной поиск понятия «точка отсчёта начисления» и поиск `T-0-4` по репозиторию с печатью
    совпавших строк и решением по каждой — `reference/ADJ_Q-18_accrual_reference_point_2026-08-12.md`;
  - принято `ADR-040`; заведена `Q-18` в оба файла одним коммитом; `T-0-3b` → `blocked`;
    `T-0-4` выведена вперёд с расширенной приёмкой.
  - **починен пятый дефект хука (`ADR-041`)**: коммит этой сессии был завален проверкой 3 на
    легитимном входе — `Q-17` архивирована дословно предыдущим коммитом, а замена строки
    «обновил» убрала её последнее упоминание из состояния; требование «назван в строках,
    ДОБАВЛЕННЫХ в архив этим коммитом» для такого идентификатора неисполнимо в принципе.
- Команды/логи ключевые: `tools/session_status.sh` — вердикт `НЕ ЧИСТО` при счётчике 1 = 0 и
  счётчиках 2–4 = 0; красным горит счётчик 5 (один неопубликованный коммит впереди `origin/main`) —
  известный ложный красный, описанный в состоянии; продолжено по разбивке, а не по итогу.
  `tools/hooks/selftest.sh` — 41 кейс, «провалено 0». `git rev-parse HEAD` — `5eb973d`.
- Отклонения от плана: **два расхождения текста запуска с репозиторием, названные поимённо
  (`ADR-027`)**. Первое: текст запуска предписывал прочитать «`07_GAPS.md` строка `Q-17`» — в
  реестре такой строки НЕТ, `Q-17` закрыта решением `ADR-037` и перенесена в `07_ARCHIVE.md`
  дословно; читалась архивная строка. Второе: текст запуска называет причиной расхождения то, что
  `CONTRACT_DATE` хранит дату первой выдачи, — репозиторием это не подтверждено (метаданные пишут
  «дата заключения договора»), и арифметика этой сессии показала, что версия одна расхождение не
  закрывает; в `Q-18` она записана кандидатом, а не причиной. Прочие утверждения текста запуска
  (восемь сверенных договоров, девятый исключён погашением, 44% перезакладываются, `T-0-4` не
  начата, запрет подгонки `ADR-039`, `PLAN_CLOSE_DATE` «от последней операции») сверены с диском и
  подтверждены — расхождений по ним нет.

## STATE_PATCH  (→ 07_STATE.md)
- Задача `T-0-3b`: todo → **blocked** (`Q-18`, снимается `T-0-4`).
- Задача `T-0-4`: получает собственную строку мандата (была в паре с `T-0-6`), класс **B**,
  выведена в очереди на первое место.
- Стенд-ап (ЗАМЕНЯЕТ блок целиком, ровно пять строк):
  - Прошлый шаг: измерительная часть `T-0-3b` исполнена, оракул не сошёлся ни на одном из восьми —
    артефакты замера и сверки, разбор `reference/ADJ_Q-18_accrual_reference_point_2026-08-12.md`.
  - Где мы: параметры начисления и портфель измерены, точка отсчёта начисления неизвестна —
    `03 §4` не записывается целиком.
  - Следующий шаг: `T-0-4` (детектор продлений + точка отсчёта), брифа нет, класс B.
  - Развилки на владельце: нет по этой сессии; названа задолженность — портфель `98` против
    «~82» в конституции.
  - Счётчик: фаза M — 5/5; фаза 0 — 7 из 16 закрыто, `T-0-3b` блокирована.
- Подробности для модели: добавлены пять строк — круг кандидатов `Q-18` и запрет записывать
  `CONTRACT_DATE` фактом; что уже известно и что не снято значениями; сужение `T-0-4` по
  `ID_PREV_CONTRACT`; незакрытые `Q-7`/`Q-8` с причиной; дефект смешанного алфавита в `02 §3`.
- Открытые вопросы: заведена `Q-18` (OPEN, гейт `T-0-4`) в `07_STATE.md` и `07_GAPS.md` одним
  коммитом. `Q-9` помечена «ждёт `Q-18`». `Q-4`, `Q-7`, `Q-8` остаются OPEN на `T-0-3b`.
- Блокеры: добавлен блокер `T-0-3b` ← `Q-18` с названной ценой (держит `03 §4`, `balance_amount`
  и все три цены фазы 3).
- В архив: нет — ни одна строка полностью не закрыта.
- updated_at: 2026-08-12
- обновил: архитектор (сессия: разбор `T-0-3b`, `ADR-040`)

## INFRA_PATCH  (→ 11_INFRA_FACTS.md)
- нет изменений: облачные ресурсы, учётные записи и параметры подключения этой сессией не
  затрагивались; к базе не было ни одного обращения.

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md)
- ADR-040: Формула баланса не валидируется оракулом, пока не известна точка отсчёта начисления:
  `Q-18` заведена, `T-0-3b` блокирована, `T-0-4` выходит вперёд с расширенной приёмкой.
  Контекст: оракул `T-0-3b` не сошёлся ни на одном из восьми договоров; правдоподобная версия о
  дате продления проверена арифметикой и одна расхождение не закрывает · Решение: `Q-18` шире
  своей версии; `T-0-3b` → blocked; урезанная приёмка отклонена как круговая; `T-0-4` вперёд с
  двусоставной приёмкой; `Q-9` ждёт `Q-18`; `Q-7`/`Q-8` не закрываются этим решением.
  Последствия: `06_INDEX` [сейчас] · `07_GAPS` [сейчас] · `07_STATE` [сейчас] · `04_ROADMAP`
  [сейчас] · `reference/ADJ_Q-18_…` [сейчас] · `02_DATA_CONTRACTS`, `03_BUSINESS_SPEC`,
  `09_GLOSSARY`, `00_CHARTER`, `05_CONVENTIONS`, `CLAUDE.md`, `08_TASK_BRIEF_TEMPLATE`
  [без правок] · бриф `T-0-4` [задачей `T-0-4`] · `00 §5` про размер портфеля
  [отдельным решением].
  Статус: accepted

- ADR-041: Пятый дефект хука — сторона архива в проверке 3 берётся объединением «добавлено этим
  коммитом» и «уже названо в архиве на HEAD».
  Контекст: коммит этой сессии завален проверкой 3 на легитимном входе, лог отказа приведён в теле
  решения; `Q-17` архивирована дословно предыдущим коммитом · Решение: объединение двух множеств;
  легитимный исход печатает свою строку; остаточная дыра названа явно; самотест расширен прямым и
  обратным кейсом.
  Замер: 45 кейсов, «провалено 0» (было 41); со старым хуком тот же самотест даёт «провалено 2» —
  краснеют ровно новые кейсы, обратный кейс красен при обеих версиях.
  Последствия: `tools/hooks/pre-commit`, `tools/hooks/selftest.sh` [отдельным коммитом] ·
  `06_INDEX` [сейчас] · `07_STATE` [сейчас] · `05_CONVENTIONS`, `CLAUDE.md`, `00_CHARTER`
  [без правок].
  Статус: accepted

## NEW_CONVENTIONS
- нет. Правило проверки 3 не менялось — менялась его механическая форма (`ADR-041`).
=== END SESSION ===
