# 11 · INFRA_FACTS — Канонический реестр ресурсов

**Статус:** LIVE, обновляется только через INFRA_PATCH. Репо публичный — endpoint'ы контура клиента и любые креды сюда НЕ пишутся (ADR-001); их место указано в колонке «Где лежит».

## GCP
| Факт | Значение |
|---|---|
| PROJECT_ID | `project-c451b48a-07ae-4de4-961` |
| PROJECT_NUMBER | `450925595005` |
| Имя проекта | My First Project (косметика, не менять — так сойдёт) |
| Владелец | корпоративный аккаунт клиента; Ilyas — **owner НА ПРОЕКТЕ** (замер 2026-08-12, `T-0-7` шаг 1: `gcloud projects get-iam-policy` печатает `roles/owner` + `roles/resourcemanager.projectMover`; `00_CHARTER.md §4` поправлен `ADR-047`). **Граница роли измерена 2026-08-13 (`ADR-049`): вверх по иерархии она не действует** — `gcloud organizations list` под этой учётной записью печатает `Listed 0 items`, а `orgpolicy.policies.create` на ресурсе проекта отказано (`IAM_PERMISSION_DENIED`). Пункт последствий `ADR-047` («операции уровня организации перестали быть стоп-условием») этим опровергнут и поправлен в `ADR-049` |
| Родитель проекта | **организация `320374923197`** (`gcloud projects describe` → `parent: {id: '320374923197', type: organization}`, замер 2026-08-13, `ADR-049`). Первый артефакт, подтверждающий красную линию `00 §4` «все ресурсы на аккаунте клиента» на уровне организации, а не со слов. Администратор организации нам неизвестен и изнутри проекта не определяется — вопрос к клиенту (`Q-21`) |
| Политика организации | **`constraints/iam.disableServiceAccountKeyCreation` действует** (замер 2026-08-13, `ADR-049`): `describe --effective` → `spec: rules: - enforce: true`; явной политики НА ПРОЕКТЕ нет (`describe` без `--effective` → `NOT_FOUND`); переопределить на проекте нельзя — `IAM_PERMISSION_DENIED` на `orgpolicy.policies.create`. `gcloud` предупреждает о замене на управляемую версию `constraints/iam.managed.disableServiceAccountKeyCreation`. **Унаследованная политика это или платформенный дефолт — замером НЕ различено** и угадыванию не подлежит |
| Биллинг | включён, `billingAccounts/01B852-005994-8F4F8E` («My Billing Account», `open: true`), замер 2026-08-13. Приведён, чтобы отказ в создании ключа не списывался на состояние проекта |
| Регион ресурсов | **`europe-west3`** (Франкфурт) — решение владельца 2026-08-12, `ADR-046`; `Q-2` закрыт. Меняется только переездом |
| Датасет BQ | `lombard_ops`, `europe-west3` — создан 2026-08-12 (`T-0-7`). Шесть таблиц (не семь — `02 §2`, поправка счёта коммитом `2c32c3b`): `loans_raw`, `events` (`PARTITION BY DATE(timestamp)`, `CLUSTER BY contract_id`), `offers`, `pricing_snapshots`, `vehicle_catalog`, `assessments`. `bq show` печатает `"location": "europe-west3"` — замер `reference/T-0-7_gcp_foundation_measurement_2026-08-12.md` |
| Бакеты | `${PROJECT_ID}-{photos,config,cfsource}` — созданы 2026-08-12 (`T-0-7`) в `europe-west3`, единый доступ на уровне бакета, `public_access_prevention: enforced` |
| SA конвейера | `lombard-pipeline@${PROJECT_ID}.iam.gserviceaccount.com` — создан 2026-08-12 (`T-0-7`). Роли: `WRITER` на датасете `lombard_ops` (dataset ACL — `bq add-iam-policy-binding` недоступен, `This feature requires allowlisting`, метод в `scripts/gcp_foundation.sh`), `roles/storage.objectAdmin` на всех трёх бакетах, `roles/secretmanager.secretAccessor` на проекте. Project-level `editor`/`owner` не выданы. **Ключ SA не создан и создан быть не может:** `keys create` отбит политикой организации (`FAILED_PRECONDITION`, замер 2026-08-13, `ADR-049`). **Механизм учётных данных агента ВЫБРАН** (`ADR-050`, 2026-08-13): федерация удостоверений через самоподписанный OIDC, статический ключ не нужен вовсе. Развилка `Q-21` закрыта, шаг 5 `T-0-8` в прежней форме снят навсегда |
| Федерация удостоверений | **Механизм учётных данных агента: самоподписанный OIDC + Workload Identity Federation** (`ADR-050`, 2026-08-13). Организацией НЕ запрещена — `constraints/iam.workloadIdentityPoolProviders --effective` → `allowAll: true` (замер 2026-08-13, `reference/T-0-8_org_policy_measurement_2026-08-13.md`). **Шаг 5-А ПОЛНОСТЬЮ ЗАКРЫТ (2026-08-13, все пять пунктов, класс B, главная сессия с подтверждением владельца).** Пул `lombard-agent-federation-pool`, локация `global`, `projects/450925595005/locations/global/workloadIdentityPools/lombard-agent-federation-pool`, `state: ACTIVE`. **Провайдер `lombard-agent-jwt-provider` СОЗДАН**, полное имя `projects/450925595005/locations/global/workloadIdentityPools/lombard-agent-federation-pool/providers/lombard-agent-jwt-provider`, `state: ACTIVE`; `attributeCondition: assertion.sub == 'lombard-agent-erp01'`, `attributeMapping: google.subject: assertion.sub`. `constraints/iam.allowedPolicyMemberDomains --effective` измерено тем же шагом → `allowAll: true`, не блокирует выдачу роли внешнему principal. `sts.googleapis.com` пришлось включить явно (`services enable`) — не был включён по умолчанию, в отличие от `iamcredentials.googleapis.com`, уже подтянутого зависимостью `T-0-7`. **Role binding ИСПОЛНЕН**: `roles/iam.workloadIdentityUser` на `lombard-pipeline@project-c451b48a-07ae-4de4-961.iam.gserviceaccount.com`, member — `principal://iam.googleapis.com/projects/450925595005/locations/global/workloadIdentityPools/lombard-agent-federation-pool/subject/lombard-agent-erp01` — единственная запись политики, других ролей SA не добавлено. Артефакт — `reference/T-0-8_federation_setup_2026-08-13.md` (разделы «Шаг 5-А, пункты 1–3» и «Шаг 5-А, пункты 4–5»). `Q-22` закрыт `ADR-051` 2026-08-13. **Значение субъекта агента (`sub`/`google.subject`): `lombard-agent-erp01`** — внутренний идентификатор, не факт о клиенте, употребляется одинаково в `attributeCondition` провайдера (подтверждено выводом `describe`) и обязано совпасть с переменной окружения `LOMBARD_AGENT_SUBJECT` конфига агента на сервере (шаг 6, не исполнен). **Значение `--issuer-uri`: `https://erp-agent.lombard-ops.invalid`** — параметр конфигурации провайдера (подтверждено выводом `describe`: `issuerUri: https://erp-agent.lombard-ops.invalid`, совпадает с `ADR-051` дословно) и claim `iss` подписываемого агентом JWT, употребляется в обоих местах побуквенно. Секретом и endpoint'ом контура клиента НЕ является: TLD `.invalid` зарезервирован RFC 6761 §6.4, в корневую зону не делегируется и не разрешается нигде — значение не указывает ни на один живой хост, домена клиенту не требуется, нового облачного и сетевого ресурса не создаётся. **Гипотеза «домен клиенту не нужен» подтверждена ФАКТОМ**: платформа приняла значение без ошибки достижимости или документа обнаружения — различающая проверка (фактическое создание провайдера) исполнена. **JWKS передан провайдеру напрямую** (`--jwk-json-path` у `create-oidc`, потолок 8 ключей) — публично читаемого issuer-URL нет, **новый публичный бакет НЕ заводился**, три бакета `T-0-7` не тронуты. Загруженный JWKS сверен с файлом сервера по `kid` (`lombard-agent-20260813`) и модулю (`n`) — совпадает побайтово. Замер способа доставки — `reference/T-0-8_jwks_delivery_measurement_2026-08-13.md` |
| Ключ подписи агента | **Новый секрет контура клиента — СОЗДАН 2026-08-13 на сервере ERP (шаг 5-Б `T-0-8`).** RSA 2048, `kid = lombard-agent-20260813`, файл `C:\LombardAgent\keys\private_key.pem`, права только на `WIN-RSAGM3JEOUA\lombard-agent-svc` (чтение) плюс `NT AUTHORITY\СИСТЕМА` (полный доступ) — предъявлено выводом `icacls`. **Значение ключа в репозиторий, `/reference` и лог не попадает никогда** (`00 §4`) — здесь только путь, права, `kid`, длина, отпечаток. Отпечаток открытой части (SHA-256): `458ff7cd668bd8220ad2f6367bb562cf03762227175c45bd3abda29209cabad2`. JWKS-файл — `C:\LombardAgent\keys\jwks.json`, содержимое (не секрет) — раздел «Шаг 5-Б» `reference/T-0-8_federation_setup_2026-08-13.md`. В Secret Manager НЕ кладётся: чтобы прочитать секрет, нужны учётные данные, которые даёт этот же ключ. **Ротация — раз в 90 дней** плюс немедленная при подозрении на компрометацию, схема — два открытых ключа в JWKS с перекрытием (`ADR-050` п. 8) |
| Secret Manager | `telegram-bot-token`, `firebird-readonly-creds`, `chat_id` — созданы 2026-08-12 (`T-0-7`) в `europe-west3`. `firebird-readonly-creds` — **версия 2 создана 2026-08-13** (шаг 5-В `T-0-8`, владелец самостоятельно в Cloud Shell под своей учётной записью, пароль через сессию ассистента не проходил): содержит реальное значение пароля `LOMBARD_RO`, версия 1 остаётся плейсхолдерной. Значение версии 2 не проверено этим шагом — проверка (чтение секрета агентом) предмет шага 7. Артефакт — раздел «Шаг 5-В» в `reference/T-0-8_federation_setup_2026-08-13.md`. `telegram-bot-token` и `chat_id` — по одной плейсхолдерной версии, реальные значения не заведены и не запрошены |

## Контур клиента (ERP)
| Факт | Значение | Где лежит секрет |
|---|---|---|
| ERP | PawnShop от «Алгоритм» | — |
| Сервер | Windows Server, LAN: 192.168.88.209 (адрес LAN и RDP совпадают, RDP 3389); адаптер Realtek PCIe GbE (один); VPN-туннель терминируется на роутере, не на сервере; работает 24/7, ИБП нет (техдолг ADR-008) | админ-креды — у Ilyas (от Исы) |
| СУБД | Firebird **2.5.9**, порт **3057**, слушает 0.0.0.0, служба `FirebirdServerPawnShop-3057` | — |
| Путь Firebird | `C:\Program Files (x86)\Firebird-2.5-PawnShop\` | — |
| Путь к .fdb | `D:\PawnShop_DOL\DB\DOL.FDB` (измерено `T-0-5`, 2026-08-11) | — |
| Креды SYSDBA | ⏳ | вне репо |
| Пользователь `LOMBARD_RO` | существует, пароль известен владельцу (подтверждено подключением); свои гранты — `SELECT` на 9 таблиц из `briefs/T-0-5.md`. **НЕ read-only фактически.** Измерено `T-0-5`, 2026-08-11: `PUBLIC` несёт `SELECT` на 107 таблицах базы (всего таблиц 108 по замеру `T-0-2`) и права записи `D`/`I`/`U` каждое на 104 объектах (шаг 6-ж, `reference/T-0-5_readonly_mode_measurement_2026-08-11.md`). Гранты `PUBLIC` не отзываются (`ADR-032`, письмо вендору снято `ADR-034`, риск постоянный). **Режим `SET TRANSACTION READ ONLY` на клиентской стороне подтверждён замером: движок его переключает** (шаг 6-е-БИС, `MON$READ_ONLY` разное между прогонами при разных номерах транзакций, тот же артефакт) — **уровень отказа ИЗМЕРЕН 2026-08-12 на нашем экземпляре Firebird 2.5.9** (`T-0-17`, `ADR-037`, `Q-17` закрыт): отказ приходит на уровне ЗАДЕВАЕМОЙ ЗАПИСИ — `SQLSTATE = 42000`, `attempted update during read-only transaction`; оператор с нулём задетых строк проходит молча и режим не измеряет. На базе клиента решающая проба запрещена `00 §4`, поэтому факт перенесён с нашего экземпляра той же версии, а не снят на прод-базе. Учётка по-прежнему не используется как ЕДИНСТВЕННАЯ защита от записи: основной контроль — белый список операторов в нашем слое — ещё не заведён. Замеры: `reference/T-0-5_readonly_measurement_2026-08-11.md`, `reference/T-0-5_readonly_tx_measurement_2026-08-11.md` (снятый шаг 6-е), `reference/T-0-5_readonly_mode_measurement_2026-08-11.md` (шаги 6-е-БИС и 6-ж), `reference/T-0-17_readonly_engine_measurement_2026-08-12.md` (уровень отказа, наш экземпляр) | вне репо (пароль) |
| Среда сервера ERP (замер 2026-08-13) | `WIN-RSAGM3JEOUA`, Windows Server 2019 Standard build 17763, **рабочая группа `WORKGROUP`, НЕ в домене** (`DomainRole: 2`, `ProductType: 3`). Корпоративного поставщика идентичности НЕТ — три независимых подтверждения (роли AD FS/AD DS/WIF `Available`, семь служб «НЕ ЗАРЕГИСТРИРОВАНА», 0 совпадений по установленному ПО при обратном контроле в 39 записей). PowerShell 5.1.17763.6189, .NET 4.8.03761; подпись и проверка RS256 в памяти получены (RSA 2048). **Python установлен шагом 5-Б `T-0-8` (2026-08-13): версия 3.14.7**, MSI-установщик с `python.org`
(`winget` недоступен на Server 2019 — метод отступления, названный заранее, сработал). **Служба
времени Windows синхронизирована шагом 5-Б `T-0-8` (2026-08-13):** первый прогон `w32tm /query
/status` дал «не синхронизировано» (индикатор помех `3`, источник `Local CMOS Clock`, код возврата
`0` — гэп наблюдения, не факт синхронизации) при рабочей группе без домена; исправлено ручной
привязкой `w32tm /config /manualpeerlist:"time.windows.com,0x1" /syncfromflags:manual
/reliable:YES`, итог — индикатор помех `0`, страта `5`. Часовой пояс `Central Asia Standard Time`, арифметика UTC сходится. Связность к `sts.googleapis.com` и `iamcredentials.googleapis.com` подтверждена (`HTTP 404` на `GET /` под штатным валидатором, `ADR-048`). Замер — `reference/T-0-8_server_identity_measurement_2026-08-13.md` | — |
| Кодировка данных | UTF-8 | — |
| Firewall | частный профиль ON; правило «Firebird 3057 (VPN+LAN only)» — inbound TCP 3057 для 192.168.88.0/24 и подсети VPN | — |
| Доступ извне | VPN WireGuard через роутер клиента (Keenetic Voyager Pro KN-3510, прошивка 5.0.x); белый, возможно динамический IP + DDNS | endpoint, порт WG и .conf-файлы — у Ilyas, вне репо |
| VPN-пользователи | peer для Ilyas + peer `lombard-connector` под автоматику | ключи — у Ilyas, вне репо |
| Служебная учётная запись агента | **Локальная учётка Windows `lombard-agent-svc` заведена 2026-08-13** (Ilyas вручную по RDP, вне сессии Claude). Без прав администратора, интерактивный вход запрещён (`SeDenyInteractiveLogonRight`), право входа как служба выдано (`SeServiceLogonRight`) — оба через `secedit`. **Пароль сгенерирован случайно и нигде не хранится**: не нужен, служба запускается SCM/планировщиком под этой учёткой без интерактивного входа (шаг 6). Назначение: (а) параметр `-ServiceAccountName` шага 5-Б — `icacls` ограничивает доступ к файлу приватного ключа подписи этой учёткой; (б) учётка запуска службы агента шагом 6. Артефакт — раздел «Служебная учётная запись агента на сервере ERP» в `reference/T-0-8_federation_setup_2026-08-13.md` | пароль нигде не хранится (не нужен) |

| Канал прод-подключения | **агент на сервере клиента** (вариант Б `01 §4`) — решение владельца 2026-08-12, `ADR-046`; `Q-3` закрыт. Только исходящий трафик, входящие соединения к контуру клиента не нужны. Реализация — `T-0-8`. **Связность ПРОВЕРЕНА 2026-08-12 и канал пригоден** (`ADR-048`, шаг 1 задачи): с сервера ERP исходящий HTTPS доходит до `storage.googleapis.com` (корректный сертификат во всех прогонах) и до `oauth2.googleapis.com` (имя покрыто `*.googleapis.com` в списке `subjectAltName`, штатный валидатор .NET сертификат принимает, `POST /token` возвращает штатную ошибку Google). Прокси не задан ни в WinHTTP, ни в WinINet; `hosts` пуст; доверенных корней в `LocalMachine\Root` — 42; Windows Server 2019 build 10.0.17763. Первый вердикт «не пройдено» был ложноотрицательным — проба сравнивала `Subject` вместо списка имён. **`secretmanager.googleapis.com` ИЗМЕРЕН 2026-08-12 (остаток `ADR-048` закрыт):** та же проба B (штатный .NET-валидатор, без переопределения) вернула `HTTP 404` на `GET /` — транспортного отказа нет, сертификат и имя хоста приняты; `404` — ожидаемое тело API-эндпоинта на пустой путь, не признак сбоя. Все три эндпоинта агента (`storage`, `oauth2`, `secretmanager`) измерены и живы. Замеры: `reference/T-0-8_agent_channel_measurement_2026-08-12.md` (снятый вердикт, дословен), `reference/T-0-8_channel_remeasurement_2026-08-12.md` (действующий, включая замер `secretmanager`) |

## Внешние источники и артефакты (`ADR-061`)

Раздел заведён по дефекту: сессия назначила формат, путь и схему объекта, устройства которого не
знала, и построила на выдумке код с настоящими логами. **Строка здесь обязана существовать ДО того,
как написан код, читающий объект.** Нет строки — стоп-условие «объект не определён», а не повод
спроектировать механизм. Путь, употреблённый в коде, обязан встречаться в этой таблице.

| Артефакт | Что это | Кто владеет | Где живёт | В каком виде | Как попадает к нам |
|---|---|---|---|---|---|
| Аудит «Блоки A–G» (июнь 2026) | Первичный аудит бизнеса клиента, на котором стоит миссия `00 §1` и метки `RF-*`. Пирамида: сырьё → аппендиксы `APP_*` по доменам → переносимое ядро `AUDIT_STATE_v*.md` → итоговый отчёт клиенту. Названо владельцем 2026-08-14 | Ilyas | **локально у владельца; место в контуре проекта назначено `ADR-063` — каталог `audit/` в рабочем дереве, целиком в `.gitignore`, кроме описи** `audit/README.md`. На дату записи файлы НЕ положены | markdown-документы (со слов владельца, артефактом не подтверждено) | владелец кладёт в `audit/`, имена не меняются; далее карта аудита в публичный репозиторий (`T-0-20`), извлечение входов — только после прочтения |
| `APP_A_portfolio.md` (Блок A) | Аппендикс уровня 2: состав портфеля по маркам и ликвидности, концентрация, аномальные залоги, перезаклады. Статус — завершён. Главное: 31% оборота неликвид/флагманы; поле «кто оформлял» пустое во всех 454 записях | Ilyas | **локально у владельца → каталог `audit/`** (`ADR-063`). На дату записи не положен | markdown (со слов владельца) | тем же путём, что и прочие артефакты аудита |
| Сырьё Блока A, уровень 0 | Реестр заключённых договоров 01.05.2025–31.05.2026 (454 договора) · история проданных невыкупов за 12 мес · ОПУ и баланс 01.01.2026–31.05.2026 со сводкой | клиент → Ilyas | **локально у владельца → каталог `audit/`**, уровень 0. На дату записи не положено | выгрузки (`xlsx`, со слов владельца) | **в проект не переносится** (`ADR-062`): доли невыкупов пересчитает витрина `T-1-6` из базы живьём и свежее июньского среза; ОПУ вне scope `00 §5` и входом справочника не является |
| Образец справочника ликвидности | Таблица, **СИНТЕЗИРОВАННАЯ моделью** в ходе аудита (владелец 2026-08-14: «скорее просто образец»). Шесть колонок: марка, модель, ликвидность, LTV макс, скупочная база, комментарий | Ilyas | в артефактах аудита | markdown-таблица | **боевыми данными не является ни при каких условиях** (`ADR-061` п.4); допустимо употребление только как ФОРМЫ, с пометкой происхождения |
| Скупочная цена по марке | Сколько перекуп даёт сегодня. Единственная величина справочника, которая из базы НЕ добывается: это рынок, а не наша история | рынок / сотрудник клиента | — | — | **постоянный механизм назван** (`ADR-062`): ведёт сотрудник клиента, актуализация ~раз в неделю; парсинг площадок продажи автомобилей — кандидат на потом, в поставку не входит. **Открыт остаток `Q-26`:** кто проставляет НАЧАЛЬНЫЙ набор до даты поставки |

## Внешние сервисы
| Факт | Значение |
|---|---|
| Telegram Bot | ⏳ создаёт **владелец** (@BotFather), значение → Secret Manager новой версией. Задача-владелец — `T-0-18` (заведена `ADR-056`) |
| Боевые чаты | ⏳ заводятся сразу, **сотрудники клиента не добавляются**, пока контур не проверен на владельце (`ADR-056`). Класс отправки определяется составом чата: нет сотрудников клиента — класс A; добавление первого переводит отправку в класс B и требует записи в этот реестр тем же днём. Задача — `T-0-18` |
| chat_id Иса/Бектур/Эльхан | ⏳ собрать, → Secret Manager/config. Задача — `T-0-18` |
| Доступ к интерфейсу PawnShop (оракул начислений) | ⏳ **запрашивается у клиента**: RDP к рабочему месту с приложением вендора, для самостоятельной сверки начислений плотными сессиями (решение владельца, `ADR-054`). Задача — `T-0-19`; endpoint и креды — вне репозитория |
| Репо | `github.com/ilyasbazarov/lombard-ops`, public |
